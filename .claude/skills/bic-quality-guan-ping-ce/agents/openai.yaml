interface:
  display_name: "BIC Quality Review"
  short_description: "Review BIC diffs, tests, and pre-test quality evidence"
  default_prompt: "Use $bic-quality-guan-ping-ce to map the current BIC diff to changed objects and backend/browser tests, build an issue-aware quality evidence matrix, and prepare the non-executing Phase 2 manifest."
