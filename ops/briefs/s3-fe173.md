# S3 任务：BIC-meta#173 + #174 — 试管架蛇形统一 + FP 容器分配完整架/烧瓶配色 + n/a 文案（两 PR）

你是 S3（实现 + PR，列车口径）。任务书 = meta#173 与 #174（读全 issue，产品实况截图为准：R1 在底 1→5，R2 反向 10→6，R15 在顶）。

仓：BIC-agent-portal，从 origin/main（≥7c5db68）切工作树 `.wt/fe173`，分支 `fix/issue-173-serpentine-rack`（PR-A），#174 在其上续切（PR-B）。
并行知会：s3-fe175 将动右侧表单 header 布局（面板骨架层）——你在架子/表格组件层，无交集；合并前 rebase 最新 main。

## PR-A（#173 蛇形统一）

1. 先核对现状：FP 源试管选择器与 CC TubeRackMap 各自行序/行内序（fe170 称'FP 不反向'，与产品实况截图矛盾——查清谁对，结论写 PR 描述）。
2. 统一设计规则：自下而上（R1 底、R15 顶）、奇数行左→右、偶数行右→左；一处布局函数，CC/FP 两处消费。
3. #170 角色着色/灰显、#168 交互不回退。

## PR-B（#174 FP 分配架 + 配色 + n/a）

1. FP 上游只读架与容器分配架均改为**完整 75 管蛇形架**（PR-A 的布局函数）：空闲孔灰显不可点，分配交互语义不变（选容器→点孔位）。
2. 烧瓶/容器高对比化学家配色（琥珀/钴蓝/棕瓶色系，废液警示红——你提案色板，PR 描述贴对照图），架上着色与图例同步。
3. 'n/a' → zh '未提供' / en 'not reported'（CC 组分表 + FP 馏分列表），或 '—'+tooltip，选一种并统一。

## 二元验收

- 布局具名测试：R1 在底、R2 行序 10 9 8 7 6（DOM 顺序断言）；CC 与 FP 同函数出布局。
- FP 两架完整 75 管 + 空闲不可点 + 容器色 token 具名断言；'n/a' 字面不再出现（zh/en 断言）。
- #170/#159 既有测试绿；typecheck/lint 0 warn/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

两 PR sha 分别评论 #173/#174；dispatch done（FACTS/JUDGMENT 分开）。
