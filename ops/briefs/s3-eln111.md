# S3 任务：BIC-agent-service#111 — ELN 报告 CC 峰表恒空（CcEvidence 反推）

你是 S3（先证实再修，列车口径）。任务书 = c12-ai/BIC-agent-service#111（读正文）+ 源头 #63 对账评论。用户裁定派发 fix（2026-07-11）。

现象：ELN 报告 CC 峰表/收集范围台账恒空。已知根因（eln63 调查）：`_apply_cc` 经 `_cc_result` 把**持久化的 CcEvidence dump** 当**原始 CCResult** 做模型校验 → schema 不匹配恒失败 → 数据全弃（台架日志 `app.eln.aggregator:_cc_result:82 CCResult parse failed` 佐证，#85 同源遗留）。

仓：BIC-agent-service，从 origin/main（≥8b2afc0）切工作树 `.wt/eln111`，分支 `fix/issue-111-eln-cc-peak-table`。

## 实现口径（PRD rule 11 + req10）

- 聚合器改从 **CcEvidence（持久化真相）** 反推峰表：peak 行（role/判定、tubes/试管范围、rt、峰面积等 CcEvidence.fractions 可恢复字段）+ 收集范围台账。
- **有损反推诚实呈现**：CcEvidence 没有的字段（如 Mind n/a 的 tube/rt 情形，#127 外部）按 req10 可见占位（未提供/—），**绝不伪造**；rt=0.0 等上游原值照实转录（rt 语义问题归 #127，本任务不修饰数据）。
- 修复点收敛在 `_cc_result`/`_apply_cc` 读取路径：接受 CcEvidence 形状（或按存储形状正确重建），**别改 CcEvidence 的持久化写路径**（rule 3 surgical）。
- 失败可诊断：仍解析不了时保留 WARN（#109 契约）+ 占位，不再静默全弃。

## 二元验收

- 用台架真实验（bic-postgres talos_agent_db，实验 7a62d870 或最新 CC 完成实验）脚本级生成 zh+en 报告：CC 峰表渲染出可恢复字段，缺失字段可见占位（真 artifact 核验，非仅单测）；
- 具名测试：CcEvidence dump 输入 → 峰表在场（回归 #85 路径）；字段缺失 → 占位+WARN（负向断言）；
- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#111 在 squash 正文 + 具名测试在 main）；
- 反推字段映射表（CcEvidence→报告列，含哪些恒占位）评论 #111。**不重启台架**（部署归 root）。

## 并行知会

- s3-fe226b 在 BIC-agent-portal（不同仓无交集）。BE 侧无其它在跑。
- 多轮 TLC 各轮板图（eln229 follow-up）与 #183 10s 超时墙不在本任务范围，勿顺手扩修。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
