# S3 任务：meta#225+#226 显示层文案批 — FP/CC 判定标签对齐 + RE 技术细节泄露

你是 S3（小型显示层修复批，列车口径）。任务书 = c12-ai/BIC-meta#225 与 #226（读正文，含用户裁定原话与截图描述）。

仓：BIC-agent-portal，从 origin/main（≥3b2c50e）切工作树 `.wt/fe225`，分支 `fix/issue-225-226-display-copy`。
两项可一个 PR 落地（squash 正文同时带 #225 #226，具名测试各自独立）。

## A. #225 FP/CC 结果表判定标签对齐

现象：FP 组分详情列表『判定结果』列写『废弃液』且 badge 样式自成一套；CC 过柱结果表是另一套。
用户裁定：**统一叫产物和废液，颜色 style 用过柱的**。

- 先找 CC 过柱结果表判定 badge 的现有组件/颜色 token，FP 结果表**复用同一组件**（DRY，不复制样式）；
- 术语统一：zh 产物/废液，en product/waste，locale 两边同步（注意 s3-fe220 正在动 locales——见并行知会）；
- 纯显示层：判定数据语义（PRD rule 11 峰分类/容器映射）不动；容器名『废液瓶』是另一概念勿改；
- 具名测试：FP 判定列文案断言（产物/废液）+ 与 CC 同组件的结构断言。

## B. #226 RE 执行参数小标题暴露后端字段名

现象：RE 表单『执行参数』小标题渲染『后端执行字段：temperature_c 和 air_pressure[].pressure_mbar。』——schema 字段名泄露给化学家。

- 改为面向化学家的业务文案（说明该节收集温度与梯度气压参数），zh+en 同步；
- 全面排查同类：grep 裸字段名/schema 路径/开发者措辞（`_c`/`_mbar`/`[]` 等模式）在用户可见文案中的其它出厂点，一并修，清单落 #226 评论；
- 具名测试：该文案不含后端字段名模式的负向断言。

## 并行知会（同仓三个在跑，必须先 ack 再动共享文件）

- s3-fe220（bic-orch:2.1）：物料 modal i18n——**正在动 `src/locales/`**，你也要加 locale 条目，动之前 dispatch send 互 ack 排好先后；
- s3-fe222（3.1）：workspaceStore tab 路由——预期无交集；
- s3-gate223（4.1）：若走 FE 分叉会动状态归约/文案——若撞结果表文件，dispatch send 协调。

## 二元验收

- tsc/biome/vitest 全绿 + CI 绿 → admin squash-merge，内容核验（issue 号在 squash 正文 + 具名测试在 main）；
- PR sha 评论 #225。**不重启台架**（部署归 root）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
