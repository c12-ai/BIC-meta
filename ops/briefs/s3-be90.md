# S3 任务：BIC-agent-service#90 — 恢复 goal-confirm 非基准行投料 pass-through 预填

你是 S3（实现 + PR，列车口径）。任务书 = c12-ai/BIC-agent-service#90（读全 issue，含用户裁定与上游修复核验链）。背景链：meta#105（上游编造，已修）→ BE#103/#136（防御性丢弃，现过时）→ 本任务（恢复预填）。

仓：BIC-agent-service，从 origin/main（≥b9ca8ee）切工作树 `.wt/be90`，分支 `feat/issue-90-restore-dosing-prefill`。

## 关键点

- 丢弃点在 #103/#136 的落点（`app/session/fast_path_handlers.py` 一带，`objective_reactant_rows_from_goal` 相关）；先 git log/blame 定位当时 diff 再动手。
- 诚实原则不许回退：上游 null/缺失 → 字段空；仅 pass-through，不本地换算不编造。
- mock 三分支（med005 / Sonogashira / #161 echo）的 goal-confirm fixture 与新行为在测试中自洽。
- #103/#136 的既有具名测试按新语义翻转/保留，PR 描述说明每条的处置。

## 二元验收

- 具名测试：真实形状响应→非基准行预填换算值；null 形状→字段空。
- 全量本地 pytest 绿；ruff/pyright 干净；CI 绿 admin-merge 留痕。
- **不重启台架**（用户正在测试中！部署归 root 同步窗口）。本地验证用 talos-pg-test-73:5455 测试库。

## 收尾

PR sha + 测试处置表评论 issue #90；dispatch done（FACTS/JUDGMENT 分开）。
