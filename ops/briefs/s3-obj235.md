# S3 任务：meta#235 主反应物选择轮次冗余 — 先调查三问，再按结论优化

你是 S3（先调查后修，列车口径）。任务书 = c12-ai/BIC-meta#235（读正文：现象 trace、三个待答问题、优化方向、验收）。

仓：BIC-agent-service，从 origin/main（≥8b2afc0）切工作树 `.wt/obj235`，分支 `perf/issue-235-objective-turn-redundancy`。

## A. 先调查（三问 file:line 级结论评论 #235，再动手）

1. parse_reaction 结果的持久化与回喂链路：目标录入首 parse 的结果存哪（L2 状态？objective params？）；主反应物选择轮的工具上下文里有没有它；LLM 重调是缺数据还是缺提示。
2. update_objective_params 的分步原因：工具 schema 是否支持一次写完（SMILES+投料+baseline 标记）；prompt/参数设计哪里引导了分步。
3. 该轮的 LLM 必要性：表单 delta（选 baseline+填量）走哪条路进来（L1→L2 事件？）；确定性落库的可行性与风险（哪些场景确实需要推理——比如用户自由文本改目标）。
- 调查用一手证据：代码 + 台架 trace/事件表（bic-postgres talos_agent_db 只读）重建该轮完整输入输出。

## B. 按结论优化（以下为候选方向，按调查结果取舍，评论里写明取舍理由）

- parse 缓存：按 rxn SMILES 键持久化，SMILES 未变不重 parse、变了必重 parse（req 8 权威性不降级——缓存的是 ChemEngine 的产出，不是自造数据）；
- update 合并：单次调用完成 baseline 标记 + 量写入；
- 确定性捷径（若调查支持）：纯表单 delta 不进 LLM 规划轮——注意别破坏现有 narrate 单源（#194）与 trace 事件契约（#165 live≡persisted）。
- **手术纪律**：不重构 L2/L3 大结构；每个优化点独立可回退。

## 二元验收

- 三问结论 + 取舍理由评论 #235；
- 同场景端到端实测对比（优化前 14s/8 事件 → 后实测值，台架真跑一轮，截 trace 数据）；
- 具名测试：SMILES 未变不重 parse（负向）+ SMILES 变更仍重 parse（回归）+ 合并 update 的行为断言；
- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验。
- **不重启台架**（部署归 root；你要台架实测对比时用只读观测 + 已部署版本做"前"值，"后"值可在本地起隔离 BE 实例测——绝不动 :8800）。

## 并行知会

- 同仓在跑：s3-eln111（ELN 聚合器，`app/eln/`）——你在 L2/objective 面，预期无交集，撞了 dispatch send 互 ack。
- s3-deploy1 在做部署资产（不动 BE 代码）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
