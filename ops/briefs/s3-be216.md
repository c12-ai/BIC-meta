# S3 任务：meta#216（P1）— 实验级状态聚合修正（fail-accept 推进 ≠ 实验失败）

你是 S3（先查后修，列车口径）。任务书 = meta#216（读全 issue）。语义基准：#5/#129 fail-accept、#128 三层（jobs.outcome×review）、#182 finale 读层修正——总览头部大概率残留一个旧 any-failed 归约点。

仓：先调查定位（可能 BE experiments 投影或 portal 归约），再定 PR 落点；BE `.wt/be216`，分支 `fix/issue-216-experiment-rollup`（FE 侧若需要另开小 PR）。
并行知会：s3-fe215 修同页 IA（不动状态语义）；s3-be212 在话术模板面——若你动到 finale/_job_ground_truth 邻域先 dispatch send 对齐。

## 纪律

1. 一手定位（结论评论 issue 再动手）：总览'失败'从哪来——experiments.stage？snapshot 里的实验级字段？FE 对 jobs 的归约？用用户这条会话（rxn，TLC 3轮 fail-accept + CC/FP completed）的真实数据核。
2. 修正语义（用户口径）：fail-accept 且推进 = 非失败（进行中/完成，可带'含未达标步骤'注记——注记措辞你提案）；真全链失败/终止仍如实失败。对照测试两态。
3. 与 #182 的 _job_ground_truth 保持同源（勿再造第二个 rollup 真相源）。

## 二元验收

fail-accept 推进实验总览≠失败 + 真失败仍失败（具名对照测试，#128 夹具）；全量 pytest（+若 FE 改动则 vitest）+ CI 绿 admin-merge 留痕。**不重启台架**（部署归 root）。本地验证 talos-pg-test-73:5455。

## 收尾

定位结论 + PR sha 评论 #216；dispatch done（FACTS/JUDGMENT 分开）。
