# S3 任务：meta#229 ELN 报告 TLC 板图占位 — 聚合器未收集 TLC 图 URL

你是 S3（先证实链路缺口，再修，列车口径）。任务书 = c12-ai/BIC-meta#229（读正文，含一手日志证据与判断）。
前情必读：BIC-agent-service#63 全部评论 + #109（squash 770a847，反应式 octet-stream 修复 + 丢图必留 WARN 契约）+ #111（CC 峰表恒空，别重复修）。

现象：BE main=e26096b 下新下载报告（实验 7a62d870，12:08:08 200）TLC 板图仍是『[ Figure 1 · TLC plate ]』占位；同实验 portal 界面有图。该次生成**无 TLC figure 拉取 WARN**（唯一 WARN 是 #111 的 CCResult parse）→ 判断为聚合器未收集 TLC 图 URL，非拉取失败。

仓：BIC-agent-service，从 origin/main（≥e26096b）切工作树 `.wt/eln229`，分支 `fix/issue-229-eln-tlc-figure`。

## A. 先证实（结论评论 #229）

- 用台架数据核对（bic-postgres:5432 talos_agent_db 只读）：实验 7a62d870 的 TLC evidence（TaskResultAnalyzedEvent.evidence / trials 侧图 URL 字段）里 TLC 板图 URL 是否在库、字段名与来源；
- 对照 `app/eln/aggregator` 取图族：rendered_rxn_url 一族接了哪些，TLC 板图为何没进 figures 列表（漏映射？evidence 结构不同？）；
- 若 URL 在库但 presigned 已过期：按 #109 已立的口径（生成时重签/即取即用）一并处理——先确认 store 侧能重签。

## B. 修复+验收

- 聚合器补 TLC evidence → figure 映射：TLC 板图（多轮时含各轮）内嵌报告『反应监测』节；
- 数据真缺时按 PRD req10 可见占位 + **必留 WARN**（#109 契约）；
- 具名测试：evidence 在场→figure 内嵌（字节级断言 word/media/ 有图）；URL 缺失→占位+WARN（负向断言）；
- 端到端：台架同实验重下 zh/en 报告各一份，真 artifact 核验 TLC 图在场（脚本级即可，不重启台架）。

## 并行知会（同仓两个在跑）

- s3-be227（bic-orch:6.1）：narrate 收尾叙述——无交集预期；
- s3-gate223（4.1）：L3 门控（若 BE 分叉）——无交集预期；撞文件先 dispatch send 互 ack。
- ⚠️ 多轮 TLC 场景与 #111（CC 峰表）都别顺手扩修，各自归档。

## 二元验收

- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（issue 号在 squash 正文 + 具名测试在 main）；
- 链路结论 + PR sha 评论 #229。**不重启台架**（部署归 root）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
