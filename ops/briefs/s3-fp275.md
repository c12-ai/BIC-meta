# S3 任务：meta#275 FP 孔位口径 — A 架编号归一化单一权威

你是 S3（先重建后归一，列车口径）。任务书 = c12-ai/BIC-meta#275（读正文：现象、用户裁定 A1-A8 合法、两口径定性、修复要求四条）。

仓：BIC-agent-service，从 origin/main（≥5e77288）切工作树 `.wt/fp275`，分支 `fix/issue-275-fp-well-id-normalization`。

现场证据重建（只读纪律：ssh orin 后只 select/docker logs，禁一切写）：session `e9bdd555-f6af-45ee-932f-4f6b7a9067a6` 的该轮事件——update_fp_containers 实际入参、随后格式错误叙述的产生层。

## 修复口径（issue 已定）

- A 架编号（A1 / A:1 / 区间 A1-A8）↔ 物理管号互转收敛到**单一权威函数**；FP 预填（CcEvidence fractions 展示串解析）与 update_fp_containers 入参共用；
- 工具入参宽收严出：两种口径都接受，落库统一物理管号；**#177 前缀语义不动**（index i=管 i+1、prefix 从管 1、缺位补 0）；
- 真非法（Z99/越界/B 架未知架号）仍拒且报错点名具体非法项（fail-loud 保留但不误伤合法输入）。

## 二元验收

- 具名测试：A 架单号/区间/混合/带冒号变体 → 正确归一落库；非法仍拒（负向）；#177 非连续集不错位回归；预填与工具两路径共用权威函数的结构断言；
- 该 session 场景夹具复现：同入参修复后预填成功；
- 全量 pytest（DB 5455 串行；MQ 族以 CI 为权威）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#275 在 squash 正文）；PR sha + 重建结论评论 #275。**部署归 root**（并入当前 BE 批）。

## 并行知会（同仓一个在跑）

- s3-be273（bic-orch:4.1）：CC 参数『说而不做』守卫——你们都可能动 `specialists/tools.py`，**动之前必须 dispatch send 互 ack 圈定各自函数区**，撞行就协调先后。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
