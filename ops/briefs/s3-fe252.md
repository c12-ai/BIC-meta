# S3 任务：meta#252 现场 HTTP 登录失败 — 非安全上下文 SHA-256 digest 垫片（保 PKCE）

你是 S3（小型精确修复，列车口径）。任务书 = c12-ai/BIC-meta#252（读正文：根因确定性、修复要求、反方案）。

现象：现场 `http://192.168.12.150:15173`（LAN 裸 HTTP = 非安全上下文）登录即抛 `Crypto.subtle is available only in secure contexts`——oidc-client-ts 的 PKCE S256 需要 `crypto.subtle.digest`。

仓：BIC-agent-portal，从 origin/main（≥8bcd7b8）切工作树 `.wt/fe252`，分支 `fix/issue-252-insecure-context-pkce`。

## 实现口径

- 应用启动早于 OIDC 初始化处：`if (!window.isSecureContext && !globalThis.crypto?.subtle)` → 安装最小垫片：仅实现 `crypto.subtle.digest('SHA-256', data)`（纯 JS SHA-256，同步实现包 Promise 返回 ArrayBuffer）；**不伪造任何其它 subtle 方法**（其它调用应照常抛错，fail-loud）。
- 安全上下文（HTTPS/localhost）**零行为变化**——垫片不装（负向断言）。
- 不引第三方 crypto 大包（一个 ~100 行的 SHA-256 实现或极小依赖均可，倾向 vendored 实现 + 注明来源与许可）；不采用 disablePKCE。
- 具名测试：垫片 digest 对 NIST/RFC 标准 SHA-256 测试向量逐一相等；模拟非安全上下文装垫片、安全上下文不装；PKCE challenge 生成路径经垫片可跑通（oidc-client-ts 的 S256 计算单测或等价断言）。

## 二元验收

- tsc/biome/vitest + CI 绿 → admin squash-merge，内容核验（#252 在 squash 正文 + 具名测试在 main）；
- 评论 #252：根因复述 + 垫片覆盖面声明。**现场镜像重建与滚更归 root**——你 merge 后 dispatch done 即可。

## 并行知会

- portal 侧无在跑会话；台架 vite 热更会带上本修复（对 localhost 无影响）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
