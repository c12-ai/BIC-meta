# S3 任务：BIC-meta#181 — 用户话语内部缩写泄漏（RE/FP）+ 下发确认句双写去重

你是 S3（先查根因后修，列车口径）。任务书 = meta#181（读全 issue）。

仓：BIC-agent-service，从 origin/main 切工作树 `.wt/be181`，分支 `fix/issue-181-stage-display-names`。
并行知会：s3-fe176b 也在 BE 动 FP 表单校验面（`.wt/be176`）——你在 narrate/话术模板面，无交集；合并前 rebase。

## 范围

1. **双写根因**：复现旋蒸下发确认，定位同一句话为何发两遍（一遍 'RE' 前缀、一遍 '旋转蒸发' 前缀）——大概率是确定性 dispatch-pending 话术（#117 族模板）与另一路径（narrate 或 stage 名替换层）各产出一次。结构性去重（单一产出口），结论先评论 issue。
2. **stage 显示名单源**：一处映射（内部码→zh 专业名/en 专业名：RE→旋转蒸发、FP→组分收集；TLC/CC 保留专业缩写），确定性模板与喂给 narrate 的上下文都消费它；grep 盘点所有用户可见话语出口确保无裸 'RE'/'FP'。
3. 红线：按根因修，不打"删一句"或"正则替换"的表层补丁（memory 纪律）。

## 二元验收

- 具名测试：旋蒸/组分收集下发确认话语单句、无裸 RE/FP（负向断言）、TLC/CC 不受影响；映射单源（两消费点引同一表）。
- 全量本地 pytest 绿；ruff/pyright 干净；CI 绿 admin-merge 留痕。**不重启台架**（部署归 root）。本地验证用 talos-pg-test-73:5455。

## 收尾

根因结论 + PR sha 评论 #181；dispatch done（FACTS/JUDGMENT 分开）。
