# S3 任务：BIC-meta PR#162 rebase 到现 main + 与 #153 合流重构

你是 S3（实现，成果推回同事分支）。对象：PR c12-ai/BIC-meta#162（Drake 的 `fix/env-up-portability`，基于 pre-#153 main，现 CONFLICTING，8 处冲突 hunks）。

## 合流裁定（root 已定，勿重议）

- **保留他的四件正交修复**：① `BIC_ROOT` 自动探测（嵌套/并排布局，Makefile+common.sh，override 优先）；② `port_owner()` 源头容忍空端口（合入后**撤掉** root 在 doctor.sh 里的点补丁 `owner5433 || true`，统一走源头修）；③ `http_code()`/`http_code_ct()` 修 000000 双输出；④ gitignore `/BIC-infra/` + CLAUDE.md 冷启注记。
- **重构掉 5433 兼容**：`app_pg_port()` 不再作为"选 postgres"的机制——#153 后单实例 `bic-postgres:5432` 是唯一拓扑。把它降级为**陈旧 .env 守卫**：doctor 读 BE/lab `.env` 的 `PG_PORT`，≠5432 时红卡 "PG_PORT=<x> 已退役（#153），改为 5432"，附修复命令；`infra_containers()`/`port_table()` 不再条件注入 talos-postgres；up.sh 固定 bic-postgres。保持 #153 已合入的 doctor 语义（5433 有监听=红卡、schema-present 守卫）不回退。

## 操作

- 工作树 `.wt/pr162`，基于 `origin/fix/env-up-portability`，rebase 到 `origin/main`（现含 `0b8b734` #153 + `0d91db0` env-seed）。
- 冲突按上述裁定逐处解；完成后 `bash -n` 全部脚本 + `make up DRY=1` 预演 + 本机 `make doctor` 真跑（**doctor 是只读的，可对活台架跑**；不得重启任何服务）。
- push 回 `fix/env-up-portability` 分支（协作分支力推需要 --force-with-lease；先 `git fetch` 确认无新提交，有新提交就先并）。
- PR#162 描述追加一段"与 #153 合流"说明（保留了什么、5433 兼容为何改守卫、引用 issue #153 与 meta 0b8b734）。**不合并 PR**——留给 Drake/用户确认后合。

## 二元验收

rebase 后 PR mergeable=MERGEABLE；`bash -n` 全绿；`make doctor` 对活台架 GREEN（42±ok/0 red 量级）；`make up DRY=1` 计划输出无 5433 痕迹；四件正交修复各有一条可指认的存活证据（diff 行）。

## 收尾

评论 PR#162（合流说明 + 验证输出摘要），dispatch done（FACTS/JUDGMENT 分开）。
