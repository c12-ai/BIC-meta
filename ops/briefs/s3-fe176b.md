# S3 任务：BIC-meta#176 items 2/3 — 全孔可分配端到端（FE+BE），以 mock 机器人为契约判据

你是 S3（实现 + PR，列车口径）。任务书 = meta#176（全部评论，尤其 comment-4936463942 的推进裁定与 s3-fe176 留下的 wire 调查 comment-4935658881）+ #177（背景矛盾）。产品口径 = Production-PRD rule 11（2026-07-10 修订：every well selectable，Mind 分类是建议性预填非门槛）。

仓：BIC-agent-service（`.wt/be176`，分支 `feat/issue-176-every-well-be`）+ BIC-agent-portal（`.wt/fe176b`，分支 `feat/issue-176-every-well-fe`）。BE 先合，FE 后合。
并行知会：s3-fe179（FormChromeCollapse）、s3-fe180（step-nav routing）都在 portal 不同组件面；FP 表单组件如与 fe180 的调查撞上（弱概率），dispatch ask 协调。

## 关键背景（前任 s3-fe176 已查明，别重查）

- BE 是唯一校验方：`fp_params_form_problems` 拒 idle 孔（pinned test test_idle_tube_label_not_assignable）；`map_containers_to_collect` 只按 upstream.rack 构建 collect_config（len==rack pinned）；lab 对 robot 原样透传。
- upstream.rack = Mind 峰引用的管集合（永无 idle）；FE 的 75 孔是 display padding。

## 实现（(Y) 语义内扩集：confirm 时把用户新增孔并入 rack，数组随之增长）

1. **BE**：idle 门放行（用户新增孔合法）；confirm 处理把新增孔并入 upstream.rack（保持 rack 与 collect_config 平行、顺序确定：按管位升序或既有序约定，写清）；旧 pinned tests 按新口径翻转（PR 描述逐条）；`.trellis/spec` 契约文档同步（rule 10）。**不动 shared-types**。
2. **FE**：全 75 孔可点（空闲孔中性色可点，分配后按容器着色）；#174 色板/图例、#173 蛇形布局不回退；确认 payload 携带增长后的 rack+分配。
3. **契约判据（验收核心）**：本地起 mock 机器人（mars_interface_mock，别碰台架 MQ——用独立 vhost/队列名或独立 rabbitmq 容器，或直接调用 mock 的处理函数做进程内验证），构造含"用户新增孔"的 FP 下发，断言 mock 的收集行为与分配一致（新增孔被收集进正确烧瓶、未分配孔弃）。若 mock 行为与预期不符：**STOP，结论评论 #176/#177，不合并**。
4. #177 保持 OPEN（真机门），PR 描述注明"mock-verified；real-robot pending Mars confirmation (#177)"。

## 二元验收

- mock 契约测试：含新增孔的下发 → mock 收集行为逐管正确（具名测试）。
- BE：全量 pytest 绿（翻转清单在 PR）；FE：全量 vitest 绿 + 交互具名测试（空闲孔点选→计数→payload）。
- 两仓 CI 绿 admin-merge 留痕。**不重启台架，不碰台架 MQ 队列**（部署归 root）。

## 收尾

两 PR sha + mock 验证证据评论 #176；dispatch done（FACTS/JUDGMENT 分开）。
