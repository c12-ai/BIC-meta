# S3 任务：meta#226 遗留 — 降级/错误/调试态开发者文案批（Wenlong 裁定处理）

你是 S3（文案批二期，列车口径）。任务书 = meta#226 的扫描清单评论（issuecomment-4942401767）『⏸ 同扫命中、本批未改』全节 + 用户裁定『开session处理226遗留待做』（2026-07-11）。一期（PR#61，正常填表面）已合，本期处理剩余全部降级/错误/调试态条目：

- `workspace.param.panel.noDataExecutor` —— 泄露 `task_params_set` + `trial`
- `workspace.param.panel.{noDataPlaceholder,noDataTitle,placeholderTlc,backendRejected}`、`workspace.param.stageWillUnlock` —— 『后端契约 / backend contract』
- `workspace.param.objective.validation.backendRejected`、`chat.toast.formValidationFallback` —— 『后端校验 / backend validation』
- `workspace.param.dynamicForm.schemaUnavailable` —— "Schema unavailable — showing payload …"
- `workspace.forms.analysis.{deeplyNested,payloadEmpty}` —— 原始 payload 调试视图 "payload"
- `workspace.result.card.evidenceLinkPending` —— "pending backend support"

仓：BIC-agent-portal，从 origin/main（≥c7a4a2d）切工作树 `.wt/fe226b`，分支 `fix/issue-226-error-state-copy`。

## 改写原则

- 面向化学家的业务语言，zh+en 同步；说清『发生了什么+现在能做什么』（如『系统尚未生成本阶段参数，请稍候或联系支持』），不堆内部名词。
- **fail-loud 语义不得软化**（rule 9 / PRD req8）：错误仍是显式错误，只换措辞不藏状态；诊断信息不丢——技术细节（字段名/schema/payload）可移到可展开详情、console 或 title 提示，不留在主文案。
- 清单评论里『➖ 判为非缺陷』三条不动。
- 具名测试：扩展一期的负向断言面到本批 key（locale 主文案无 snake_case 字段名/backend/schema/payload 模式；白名单注明可保留的展开详情位）。

## 二元验收

- tsc/biome/vitest + CI 绿 → admin squash-merge，内容核验（#226 在 squash 正文 + 具名测试在 main）；
- 改写前后对照表评论 #226（全部条目 key→旧→新）。**不重启台架**（部署归 root）。

## 并行知会

- s3-eln111 在 BIC-agent-service（不同仓无交集）。portal 侧无其它在跑。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
