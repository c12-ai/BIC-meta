# S3 任务：meta#242 样品管选择类型完整性 — armed 一致 + ?管补录 + 未 arm 拒选

你是 S3（实施已裁定的设计，列车口径）。任务书 = c12-ai/BIC-meta#242（读正文：用户裁定原话、事实前提、实施规则五条、验收）。注意 #239 后续裁定 B2：配对=纯粗都要有，**无排序要求**（PRD rule 9 现行文本为准，fa8def7）。

现象基线（用户 2026-07-11 现场截图确认仍是旧行为）：armed 纯品后点『?』存量管只是普通选中（深蓝），类型仍未知；未 arm 也能随意选『?』管。

仓：BIC-agent-portal，从 origin/main（≥2e34153，含 fe220 chip/fe237 refresh/#239 配对提示）切工作树 `.wt/fe242`，分支 `feat/issue-242-type-integrity`。

## 实施规则（已裁定，勿重议）

1. **armed（已选纯/粗）**：点空格=放置该类型（现状保留）；点『?』存量管=按 armed 类型**补录**（staged：chip 立即反映新类型，Confirm 时写 properties.purity，Cancel 恢复『?』）；点**异类型**管=拒选+警告提示（『该管为粗品，切换到添加粗品后可选』类文案，zh+en）。
2. **未 arm**：有类型管正常可选；『?』管拒选+警告引导先选类型。
3. 选中态视觉与类型 chip 正交保持（fe220 的 2×2 可辨）；补录管 chip 即时更新。
4. **#206 四不变量不破坏**：补录也是 Confirm-gated（draft 阶段零 lab 写）、Cancel 全恢复。
5. **PRD 同步**（rule 10）：Production-PRD rule 8 增补选择侧类型语义（armed 补录/未 arm 拒选），meta 仓 PR 与 portal PR 同批（PRD PR 你提、评论 @root 复核后 admin merge）。

## 验收（具名测试）

- armed 点 ? = staged 补录（Confirm 写回 / Cancel 不落库，负向）；
- armed 点异类型 = 拒选+警告文案断言；
- 未 arm 点 ? = 拒选+警告；未 arm 点有类型管 = 正常可选；
- 配对校验（#239 B2）与本改动共存：补录后的类型计入配对判定；
- tsc/biome/vitest + CI 绿 → admin squash-merge，内容核验（#242 在 squash 正文）；PR sha 评论 #242。
- **部署归 root**（台架热更 + 现场 field 镜像滚更）。

## 并行知会

- s3-fe258（bic-orch:5.1）在 portal 的 session 列表面 + BE session 接口——与 MaterialPreparationPanel 无交集预期，动前 dispatch send 互 ack。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
