# S3 任务：meta#268 输入框占位硬编码英文 — 入 i18n + 反向裸串扫描

你是 S3（i18n 补漏，列车口径）。任务书 = c12-ai/BIC-meta#268（读正文）。

现象：中文界面下输入框占位显示英文。定位已给：`src/pages/chat/ChatPanel.tsx:38` `const chatLockedPlaceholder = 'Waiting for Talos to reply…'` 硬编码未走 t()。

仓：BIC-agent-portal，从 origin/main（≥59a9f63）切工作树 `.wt/fe268`，分支 `fix/issue-268-hardcoded-placeholder`。

## 要求

1. 该串入 locale（zh『等待 Talos 回复…』/ en 保持原文），走 t()；具名断言 zh 下该占位为中文。
2. **反向全扫**（这是本任务的主要价值）：fe220 的 used-key 门禁只查 t() 引用的 key 是否有定义，抓不到根本没走 t() 的裸串。全扫 src/ 组件里用户可见的硬编码英文字面量（JSX 文本节点、placeholder/title/aria-label/alert 等属性），逐个判定：用户可见 → 入 locale；开发者态（console/错误码）→ 留下并注明。清单（修复项+豁免项+理由）评论 #268。
3. 若能落一个可持续门禁（biome/自写 lint 或测试：JSX 可见文案禁裸英文串，白名单机制），加上；不可行则在 #268 写明为什么。
4. tsc/biome/vitest + CI 绿 → admin squash-merge，内容核验（#268 在 squash 正文 + 具名测试在 main）。

## 并行知会

- portal PR#72（fe242，等 Wenlong review）未合——你从 main 切，若它先合并你 rebase；不碰它的文件（MaterialPreparationPanel/TubeSelectorGrid）除非全扫命中，命中则只改文案行。
- s3-be266 在 BE 仓（无关）。

## 收尾

**部署归 root**（台架热更 + 现场 field 镜像滚更合批）。dispatch done（FACTS/JUDGMENT 分开）。
