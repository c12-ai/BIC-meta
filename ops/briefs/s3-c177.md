# S3 任务：#177 裁定全局对账 — collect_config 语义（i+1 前缀式）在所有仓的记载/实现一致化

你是 S3（先盘点后修，列车口径）。权威裁定 = meta#177 comment-4936558789（Mars via Wenlong，2026-07-10）：**collect_config[i] = 第 i+1 号物理管**，前缀恒从 1 号管起，变长合法、缺号补 0（弃）、不能任意起点。PRD 已对齐（meta 6276005）。

任务：**统揽全局**，把与该语义不一致的记载（docstring/注释/示例/spec 文档）与实现逐处找出并修正。

## 范围与分工边界

**排除**（s3-fe176b 正在 `.wt/be176`/`.wt/fe176b` 做，勿碰）：BE `app/events/form_payloads.py` 的 map_containers_to_collect 构建修复、fp_params_form_problems 门、FP 表单 FE、两者的直接测试。

**你的盘点面**（各仓 grep collect_config/flasks/disposition 全量清点，逐处判定"一致/需改/fe176b 已管"三态并列表在 issue 评论）：

1. **BIC-shared-types**（`.wt/c177`，分支 `docs/issue-177-collect-config-semantics`）：`re.py` CreateFPTaskRequest.collect_config 的 docstring/Field description + 契约 example 注释——明确写入 i+1 前缀语义（引用 Mars 裁定日期）；example 数组标注"= tubes 1..len 的处置"。校验器 `collect_config[i]∈[0,len(flasks)]` 与语义相容，确认后不动逻辑。跨团队仓：PR 描述引用 meta#177，正常列车合并（纯文档注释，CI 绿）。
2. **BIC-lab-service**：task_resolver.py:172 附近 'tube indices to collect from' 类注释——按处置数组语义改写；其余 collect 相关注释/文档/测试夹具的语义性描述。逻辑透传不变（除非发现真校验错位——那要先评论上报）。
3. **BIC-agent-service（fe176b 排除面之外）**：collect_config 的其他消费点——FP 结果合成（15ml/管数学从哪读管集）、ELN 报告 FP 段、快照/预填投影、`.trellis/spec/` FP 契约文档——逐处核对按 i+1 前缀读写；发现读侧按旧平行语义解读的真 bug，修复 + 具名测试（与 fe176b 的构建侧修复互补，注意 rebase 顺序：若它先合，基于其 main）。
4. **mars_interface_mock**：canned 回放不模拟逐管（fe176b 已查实），只核注释/README 不要写错语义；不加逐管模拟（YAGNI，除非一行注释能澄清）。

## 二元验收

- 盘点清单评论 meta#177（关闭状态下追评即可）：每处引用 file:line + 三态判定。
- 各仓改动 PR：CI 绿 admin-merge 留痕；BE 若有读侧修复须具名测试（非连续管集下读侧无错位）。
- **不重启台架**（部署归 root）。

## 收尾

盘点清单 + 各 PR sha 评论 meta#177；dispatch done（FACTS/JUDGMENT 分开）。
