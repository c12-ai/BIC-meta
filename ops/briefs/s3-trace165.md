# S3 任务：BIC-meta#165（P1 结构性）+ #166（UX）— 追踪块双源不对称重构 + 工具调用折叠

你是 S3（先调查后实现，列车口径）。任务书 = meta#165 与 #166（读全 issue）。用户红线：**这是反复复发的家族问题，要结构性修复，不要点状补丁**。

涉及仓：BIC-agent-service（事件持久化/快照）+ BIC-agent-portal（追踪块渲染）。工作树各自 `.wt/trace165`。

## 阶段 1：调查（结论先落 #165，再动手）

- 在台架（只读！用户可能在测试；**不许重启任何服务**）或本地自起实例上跑一条真实 turn，逐字段对账三份数据：live SSE 事件序列 / talos_agent_db session_events 行 / 快照 API 返回。
- 输出：缺失/变形点清单（think 文本是否持久化？逐事件 duration/relative-ts 是否上库？快照重建把什么合并成了 3 个事件？portal 是否两套渲染路径？）+ 结构性设计（单一事实源：补全持久化元数据，portal 收敛一套渲染）。
- 台账查询走 bic-postgres:5432 的 talos_agent_db（只读 SELECT）。

## 阶段 2：实现（依设计拆 PR，BE 先 FE 后）

- BE：事件写入补全追踪元数据（duration/relative-ts/think 段落），快照返回与 live 同形；涉及 FE↔BE 契约变化必须同步更新 `.trellis/spec/` 契约文档（rule 10）。
- FE：单一渲染路径消费同形数据；顺手落 #166——工具调用默认折叠一行（工具名+状态徽标），点开展开参数/结果；#166 里'内部指令文本是否该给用户看'的问题给出你的调查结论（若判定裁剪，属 BE 输出面，单独说明范围再做）。
- 兼容：历史已存事件缺新元数据 → 渲染诚实降级（不显示假 0.0s，显示'时间未记录'类空态），不造假时间。

## 二元验收

- 同一 turn：live 记录的事件数/总耗时/逐事件相对时间戳，刷新后逐项相等（具名测试断言两源同形 + 回退即红）。
- 追踪块默认无 JSON 大块，展开/收起具名测试。
- 各仓全量测试绿 + CI 绿 admin-merge 留痕（BE 的 CI 现在是真 CI）。**不重启台架**（部署归 root）。

## 收尾

调查结论 + 各 PR sha 评论 #165（#166 引用带上），dispatch done（FACTS/JUDGMENT 分开）。
