# S3 任务：BIC-meta#168 — 实验证据图片 lightbox 放大查看

你是 S3（实现 + PR，列车口径）。任务书 = meta#168（读全 issue）。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe168`，分支 `feat/issue-168-evidence-lightbox`。
并行知会：s3-trace165 在 chat 追踪块、s3-fe167 在 workspace 执行日志——你的面在证据图片组件（TLC 板/CC 结果图），无交集；开工前基于最新 origin/main。

## 设计约束（KISS）

- 一个可复用的 Lightbox 组件：点击证据图 → 全屏遮罩 + 原图（presigned 原图 URL，不是缩略图）；滚轮/双击缩放、拖拽平移、Esc 或点遮罩关闭。
- 优先看仓里有没有既有 lightbox/modal 基建可复用；确需新依赖时选轻量且 pnpm 生态常规的（在 PR 描述里说明理由），别为单一功能拖重库。
- 盘点现有渲染证据图的组件（TLC 板识别面板、CC 结果图卡等）统一接入，一处实现。
- 标框（Rf bbox 叠加）在放大视图保持可见——若标注是 DOM 叠加而非烧进图片，lightbox 里也要同步渲染叠加或说明取舍。

## 二元验收

- TLC 板图与 CC 结果图：点开/缩放/平移/关闭 具名交互测试（vitest + testing-library）。
- typecheck / lint 0 warn / vitest 全量 / build 绿；portal CI 绿 admin-merge 留痕。
- **不重启台架**（部署归 root）。

## 收尾

PR sha 评论 #168；dispatch done（FACTS/JUDGMENT 分开）。
