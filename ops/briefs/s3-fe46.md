# S3 任务：portal#46 一次性综合更新 — 落地 #206 模型 + 恢复 #188 语义 + 合最新 main（直推 PR 分支）

你是 S3（协作修缮，**直接 commit 到同事 PR 分支** `codex/upgrade-lablogistic`，用户授权；成功先例=pr100split 的纪律）。裁定链：meta#206 方案 + 用户四项裁定（①允许受控放置 ②Option A 客户端暂存 ③一次性在 #46 上完成 ④PRD rule 8 增补已落，见 Production-PRD rule 8 supplement 2026-07-11）+ s3-prreview 对 #46 的 review 评论。

## 同事分支协作红线

- **禁 force-push**：`git merge origin/main` 进分支（解冲突时**今晚 main 的全部工作为准**：#188 CC maintain-then-select、issue-188 守卫测试恢复、#173 蛇形、#174 色板、#176 全孔、#179/#175 折叠、#180 路由、fe201 工具行），新实现作为叠加 commit。
- 开工 `git fetch` 确认分支无新推进；作者若在动，dispatch ask 停等。commit message 引 #206/#46 review/PRD 增补，让作者可追溯。
- **不合并 PR**（合并留用户/作者）。

## 实现（#206 推荐方案 = 类型先行 + 草稿-提交暂存面，Option A）

1. **TLC modal 状态机**（#206 P 段）：空白/库存绿/选中蓝(纯品/粗品有区分标识)五态；点击行为：库存绿→选中（纯/粗品类型先行 arm 后才可点，含绿格与空格）；再点选中→回原态（绿回绿、暂存放置回空白）；无类型 arm 时空格不可点。
2. **草稿-事务**：modal 内全部操作 staged（含暂存放置）；**Confirm 才批量写 lab**（放置=创建库存+选中；选择=纯 draft 提交到 params）；**Cancel 全弃、lab 零写入**——用户三条硬性要求逐条具名测试。
3. **恢复 #188**：#46 分支上被回退的 CC maintain-then-select、维护开关、issue-188 守卫测试全部恢复（merge 冲突解决时保 main 侧 + v2 item-card 视觉可保留但语义按 #188/#206）。
4. **spec 同步**（Rule10）：portal `.trellis/spec/ui/L3/form.md` 按 #206 增补口径改写（#46 分支上那段自称 rule-8 reversal 的 spec 改正）；PRD 已由 root 更新勿动。
5. #46 原有价值保留：putTrialParamsDraft 持久化、tube positional 编号修复、issue-90 测试。
6. 就绪校验/2-4 管同行规则衔接（#206 §衔接段）。

## 二元验收

- 用户三要求具名测试：①绿格与空格（类型先行后）均可点且行为一致；②Cancel 恢复原状（绿仍绿、暂存放置消失、lab 零调用断言）；③无类型不得落空格。
- deselect 既有管零库存写入（与 fe207 止血同断言，merge 后统一）；#188/#173/#174/#176/#179 具名测试全绿。
- 全量 vitest + typecheck/lint/build 绿；PR head CI 全绿；mergeable=MERGEABLE。**不合并、不重启台架**。

## 收尾

综合更新说明（改了什么/保留了什么/恢复了什么）评论 #46 @作者 + 评论 #206；dispatch done（FACTS/JUDGMENT 分开）。
