# S3 任务：meta#270 选主反应物后化合物名丢失 — update 按 smiles 合并保富化

你是 S3（服务端数据治理修复，列车口径）。任务书 = c12-ai/BIC-meta#270（读正文：现场 DB 一手证据、机制定性、修复要求）。

现象：parse 后两化合物均有名（chem-service 富化）；用户选主反应物 → LLM 调 `update_objective_params` 重发 reactants，未往返的富化字段（name/structure_url）被原样覆写为 null。现场 session 9f8af5ae 实锤：baseline 项名在、另一项 name=null。

仓：BIC-agent-service，从 origin/main（≥2d824e2）切工作树 `.wt/be270`，分支 `fix/issue-270-preserve-enrichment-on-update`。

## 修复口径

1. `update_objective_params` 写 reactants 改**按 smiles 合并**：入参字段 null/缺省 → 保留既有值（富化字段 name/structure_url 重点）；显式变更（量/当量/角色/baseline）照常生效；smiles 变化视为新化合物（旧富化不得串位）。
2. 与 #235 的 reseed/parse 缓存三处口径核对一致（factory._objective_draft_seed / parse 缓存 / update 合并——名字在三处都不丢）。
3. 可选兜底（做则遵循 #55 静默降级契约）：读到 name=null 且 chem 可用时补拉 compound-names。
4. 服务端治理为主，不打提示词补丁。

## 二元验收

- 具名测试：部分更新不冲富化（正向）/ smiles 变更旧富化不串（负向）/ 显式改名仍可行（回归）/ #235 场景 reseed 后名在（集成）；
- 全量 pytest（-m 'not real_llm and not requires_bench'，DB 5455 串行；MQ 族以 CI 为权威）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#270 在 squash 正文）；
- PR sha 评论 #270。**部署归 root**（与 be266/fe268 合批）。

## 并行知会

- s3-fe268 在 portal（无关）；BE 仓无其它在跑（be266 已收档）。与 #235 的 objective.py/factory.py 同区，从最新 main 切即含其变更。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
