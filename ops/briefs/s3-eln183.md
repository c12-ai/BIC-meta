# S3 任务：BIC-meta#183 — ELN 下载迟缓根因+UX；报告字段按现况刷新

你是 S3（先查后修，列车口径）。任务书 = meta#183（读全 issue）+ 历史语境：BIC-agent-service#35（ELN BE）、#54（chem-service 缺依赖时代）、#55（degrade 契约）、meta Production-PRD requirement 10。

仓：BIC-agent-service（`.wt/eln183`，分支 `feat/issue-183-eln-refresh`）+ BIC-agent-portal（若需加载态，`.wt/eln183fe`，分支 `feat/issue-183-download-ux`）。
并行知会：s3-c177 可能触到 ELN 报告的 FP 段（collect_config 读侧对账）——开工先看它在 meta#177 的盘点评论，若它已改 ELN 文件则基于其合并后 main；s3-be181 在话术层无交集。

## A. 下载迟缓

1. 台账取证：计时拆解一次真实报告请求（聚合/富化/图片拉取/docx 构建各占多少），结论评论 issue。
2. 处置阶梯：可并行化/缓存的先做（如证据图并发拉取、chem-service 批量调用）；仍 >1.5s 则 FE 加载态（按钮 spinner+生成中+禁重复点+失败态）。已知事实：BE .env CHEM_SERVICE_HOST=127.0.0.1:8010 已配且服务在跑——富化调用真实发生，可能是耗时来源之一。

## B. 字段刷新（req 10 omit-never-fabricate 不变，'可得'边界更新）

1. username：报告头实验人/导出人用会话真实身份（Keycloak JWT 的用户名——查现有 current_user 链）。
2. 反应式图：goal-confirm/rxn-parse 的 rendered_rxn_url（presigned PNG）嵌入报告；URL 过期/不可达→该图缺省不阻塞（req 10 契约）。
3. FW/moles：核实 chem-service 连通后端到端出现在报告；不可达静默缺省（#55 契约测试保持）。
4. 盘点 #35/#54/#55 讨论中其余'当时不可得'字段：TLC 板图/CC 仪器图（presigned 证据链路已在）可嵌则嵌；rt 等 Mind 未修字段保持 absent。盘点清单评论 issue。
5. zh/en 双版同步；报告是确定性聚合，无 AI 参与（req 10 红线）。

## 二元验收

- 点击→1s 内可见反馈；生成中/失败态具名测试（若 FE 改动）。
- 报告实测含 username/反应图/FW+moles（服务可达）；不可达两态具名测试（absent 不造假）。
- 全量 pytest（真 CI）+ portal 门禁绿；CI 绿 admin-merge 留痕。**不重启台架**（部署归 root；台架实测下载留给 root/用户终验）。

## 收尾

耗时拆解 + 字段盘点 + PR sha 评论 #183；dispatch done（FACTS/JUDGMENT 分开）。
