# S3 任务：BIC-agent-service#86 — test_lab_status 族 round_index 形状跟修 + 拆 CI skip 掩盖

你是 S3（实现 + PR，列车口径）。任务书 = BIC-agent-service#86（读全 issue）。

仓：BIC-agent-service，从 origin/main 切工作树 `.wt/be86`，分支 `fix/issue-86-lab-status-shape`。

## 范围

- 复现：本地全量 pytest 定位 test_lab_status 族 result 步骤形状失败（ST-1 45032c9 上 wire 的 round_index / op 级 step_events 字段漂移所致）。
- 跟修测试期望形状（对齐 shared-types 45032c9 + lab main 3c03a84 的实际序列化；0-based round_index，END_TLC/非 TLC=None）。
- **拆掉 CI 对该族的 skip**（skip 掩盖真失败违反 fail-loud）；查 CI workflow/pytest 标记里的 skip 来源并移除。
- 若发现漂移不止测试期望而是运行码真缺陷，停下评论 issue 报告，等 root 裁定，不擅自扩范围。

## 二元验收

- 该族在本地全量 pytest 绿；CI 上该族真实执行（skipped 计数不再包含它）且绿；全量单测绿；CI 绿 admin-merge 留痕。
- **不重启台架**。

## 收尾

PR sha + skip 拆除位置 评论 issue #86；dispatch done（FACTS/JUDGMENT 分开）。
