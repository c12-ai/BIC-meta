# S3 任务：BIC-meta#176 — FP 容器分配左右布局 + 所有孔位可分配

你是 S3（实现 + PR，列车口径）。任务书 = meta#176（读全 issue）。产品口径已定：Production-PRD.md rule 11 UI 段 2026-07-10 修订（side-by-side + every well selectable，Mind 分类 = advisory pre-fill 非 gate），照 PRD 执行，勿重议。

仓：BIC-agent-portal，从 origin/main（≥ae5e61f）切工作树 `.wt/fe176`，分支 `feat/issue-176-fp-assign-layout`。
注意与 #175 刚合的 FormChromeCollapse 骨架层协同（你在表单内容区排布，无冲突但 rebase 最新 main）；#173/#174 的 serpentineRackLayout 与色板直接复用。

## 范围

1. 左右布局：容器选择列（chips + 添加烧瓶 + 图例）在左，完整蛇形架在右；窄视口（<720px）允许回退上下堆叠（响应式）。
2. 所有孔可点：撤掉 idle 不可点逻辑——任意孔可分配到激活容器/从容器移除；空闲孔中性色可点，分配后按容器着色；选中列表/总数联动不变。
3. dispatch payload 语义核对：disposition 数组本就全架覆盖，确认空闲孔分配后正确进数组（既有序列化测试扩一条含'原空闲孔被分配'的用例）。

## 二元验收

- 900px 视口容器控件与架子并排（布局具名测试或截图实证）；窄视口降级不破。
- 原空闲孔可点击分配并进 payload（具名交互+序列化测试）；#174 着色/图例不回退。
- typecheck/lint 0 warn/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

PR sha 评论 #176；dispatch done（FACTS/JUDGMENT 分开）。
