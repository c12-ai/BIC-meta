# S3 任务：meta#255 话术泄露『基线』术语 — 扩展 #240 治理面

你是 S3（小型 narrate 治理修复，列车口径）。任务书 = c12-ai/BIC-meta#255（读正文与用户裁定原话）。

现象：操作员请求话术『主反应物（基线）的 SMILES 是…』。裁定：**用户可见话术不得出现『基线/baseline』**——它是 objective 模型 reactants 的内部 role 名，『主反应物』已完整表意。

仓：BIC-agent-service，从 origin/main（≥18efcd6）切工作树 `.wt/be255`，分支 `fix/issue-255-no-baseline-jargon`。

## 要求

- 定位该话术产出链（大概率 narrate 上下文携带 role=baseline 或治理指令/提示词示例里写了『基线』——#240 的 d437158 刚做过同族治理，沿其结构扩展）；
- 数据侧治理优先：进 narrator 的 reactants 引用用『主反应物/其它反应物』业务称谓，role 枚举值不外显；顺查其它 role/内部枚举外显点，清单落 #255；
- 具名负向断言：用户可见话术无『基线』与『baseline』模式（扩展 #240 的 test_narrate_field_and_rf_leak 断言面或新增同族测试）；
- 与 #235（obj 面 18efcd6）、#240 无回归（既有套件全绿）。

## 二元验收

- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#255 在 squash 正文 + 具名测试在 main）；
- 产出链 + 同族外显清单评论 #255。**部署归 root**（台架与现场同批滚更）。

## 并行知会

- s3-sse254 可能动 BE 的 sse ticket/keepalive 面——与 narrate 无交集预期；撞了 dispatch send 互 ack。
- s3-fe252 在 portal（不同仓）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
