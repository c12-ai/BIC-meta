# S3 任务：meta#273 说而不做 — 用户补参被复述但未写表单，结构性保障

你是 S3（先重建证据再结构性修复，列车口径）。任务书 = c12-ai/BIC-meta#273（读正文：现象、定性方向、修复取舍框架）。

现象：CC 参数阶段 agent 追问粗品质量 → 用户答 5mg → 模型话术复述 5mg 但该轮**零工具调用**，表单未写。现场 session `e9bdd555-f6af-45ee-932f-4f6b7a9067a6`（现场 talos_agent_db 只读可查——ssh orin 后 docker exec bic-postgres，凭据在容器 env；**现场只读纪律**：只 select/docker logs，禁一切写）。

仓：BIC-agent-service，从 origin/main（≥2d824e2）切工作树 `.wt/be273`，分支 `fix/issue-273-ack-without-write-guard`。

## A. 先重建（结论评论 #273）

- 从 session_events 重建该轮：用户消息、进模型的完整上下文（含 awaiting 状态如何表达）、模型原始输出（复述 5mg 无 tool_call）；
- 判定诱因：追问后的状态在上下文里是否可被模型看见？工具 schema 是否清晰？该模型（Qwen3.5-27B）在此类轮的历史成功率（同库其它轮对照）。

## B. 结构性修复（issue 给了两个方向，按证据取舍并写明理由）

1. **说而不做守卫**：awaiting-param 状态 + 用户消息含可解析参数值 + 该轮无对应写入调用 → 结构级强制（re-prompt 重试该轮/注入系统纠正），非提示词乞求；
2. **确定性捷径**：agent 明确追问某字段后，用户的直接数值回答确定性解析落库（#235 框架、PRD req 3）——若走此路注意单位/歧义处理（『5mg』可解析；『大概5毫克吧』需模型），混合策略可取；
3. 提示词强化仅辅助层；勿动 narrate 单源与 #194 结构锁。

## 二元验收

- 具名测试：awaiting+数值回答+零调用 → 保障触发（正向）/ 正常调用不误触（负向）/ 捷径路径（若做）单位解析断言；
- 该轮场景夹具级复现：修复后同输入产生表单写入；
- 全量 pytest（DB 5455 串行；MQ 族以 CI 为权威）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#273 在 squash 正文）；PR sha + 重建结论评论 #273。**部署归 root**。

## 并行知会

- s3-be270（bic-orch:2.1）在同仓 objective/富化合并面——你在 CC 参数/守卫面，若撞 specialist 公共文件先 dispatch send 互 ack。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
