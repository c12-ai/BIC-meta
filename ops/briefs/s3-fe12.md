# S3 任务：#128 FE-1（读侧换源）+ FE-2（轮=tab IA）· 两个 PR，列车口径

你是 S3（实现 + PR）。依赖链已全部就绪：ST-1（shared-types 45032c9）✓ · LAB-1（lab main 3c03a84）✓ · BE-1 三切片 ✓（PR-1 verdict 投影 cf6209b · PR-2 jobs.outcome/review 884d767 · PR-3 trials.execution_status 732fb2b，收尾对账表在 BIC-meta#128 comment-4933206105）。台架 BE 已跑 732fb2b（两迁移已应用）。

仓：BIC-agent-portal，从 origin/main（02fe2f7）切工作树 `.wt/fe12`，分支 `feat/issue-128-fe1-read-source`（FE-1 先行），FE-2 在其上或续切分支，两个独立 PR 交付。

## 任务书（权威源）

BIC-meta#128 的阶段 1 拆分表 + BE-1/LAB-1 收尾评论。读全 issue 评论后再动手；wire 形状以 shared-types 45032c9 + BE/lab main 实际序列化为准（不确定就对着台架真响应核，8800/8192 在跑最新 main）。

## 范围

**PR-A（FE-1 读侧换源）**
- 横幅/步骤徽标读 L1 `execution_status`（不再从 status 反推）。
- 结果卡读顶层 `verdict` + `review`（BE-1 PR-1 投影字段）。
- "已自动重试"读 `superseded`。
- **删除** `analysisVerdictFailed` 形状嗅探与 `isTerminalFailStatus` 反推（FE-0 止血产物按设计整体删除）。

**PR-B（FE-2 轮=tab IA）**
- AttemptSwitcher 提升为 TLC 步骤内轮级导航；任务/监控/结果三面共享选中轮态。
- 执行日志按 trial（round_index）切片渲染，含 op 级 step_events（LAB-1 已上 wire：0-based，END_TLC/非 TLC=None）。
- 步骤级 L3 汇总条（jobs.outcome × review）。
- CC 步骤无退化（具名回归断言）。
- 注意与已合入的 #159 step-nav 编排（527137e：derive-routing + step-nav-orchestrator 边沿门）协同，不得回退其四条验收行为。

## 二元验收（每 PR）

- typecheck PASS + lint 改动文件 0 warn + vitest 全量绿（含新增具名测试：FE-1 换源后旧谓词删除即红的守卫、FE-2 轮切片/汇总条/CC 无退化）+ build PASS。
- portal 无仓级 CI（缺口 #144）：以上本地门禁全绿 + 独立 code-reviewer subagent 过一遍（≥80 置信度问题为零）后 admin squash-merge 留痕。
- **不重启台架**（部署归 root 统一同步窗口）。CDP/浏览器自起实例验证可用，不动 :5173。

## 收尾

两 PR sha + FE-1 reader 换源对账表（每个删除的嗅探点/换源点打勾）评论 BIC-meta#128；dispatch done（FACTS/JUDGMENT 分开）。
