# S3 任务：meta#222 TLC 完成时机 tab 自动跳转时序 — 解析完成前不离开任务 tab

你是 S3（先调查后修，列车口径）。任务书 = c12-ai/BIC-meta#222（读正文，含用户原话与期望不变量）。

现象：TLC 实验完成/出结果时先跳一次任务 tab，结果解析完成后再跳结果页。期望：**结果（解析）出来之前，默认保持在任务 tab，无中间跳转**；解析完成后按现有口径切结果页；用户手动切换的 byUser 保护不变。

仓：BIC-agent-portal，从 origin/main（≥3b2c50e）切工作树 `.wt/fe222`，分支 `fix/issue-222-tab-routing-timing`。

## A. 先调查（结论评论 #222）

- 重建完整事件→路由链：completion 类事件（execution 终态）与 TaskResultAnalyzed 各自触发哪次 `selectLifecycle`/路由翻转；锚点 `src/stores/workspaceStore.ts` ~365 'Panel + tab routing'、~264 `awaitingResultReview` 停留注释、routing/monitor-follow/analysis-nav 三个测试文件。
- 查与 #82 pin、#216 furthest-advanced 选择器的交互；确认中间跳转的确切触发事件与条件分支。
- 用台架真实事件序列核对（bic-postgres talos_agent_db 只读事件表 或 BE 日志），别只靠代码推演。

## B. 修复

- 实现时序不变量：解析完成（结果卡实际可看）之前，自动路由不得离开当前 tab；解析完成后的切结果页保持现状。
- 具名测试：completion 先到、analyzed 后到的窗口期内无中间跳转（负向断言）+ byUser 手选不被覆盖回归 + 现有 routing 套件全绿。

## 并行知会

- s3-fe220（物料 modal i18n，MaterialPreparationPanel+locales）同仓在跑，预期无交集；若撞 locale/公共文件，dispatch send 协调。
- s3-gate223（下游锁定门控）在查 BE/FE 状态一致性，若它也要动 workspaceStore 状态归约，先互 ack 再动。

## 二元验收

- tsc/biome/vitest 全绿 + CI 绿 → admin squash-merge，内容核验（issue 号在 squash 正文 + 具名测试在 main）；
- 调查结论（确切触发链）+ PR sha 评论 #222。**不重启台架**（部署归 root）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
