# S3 任务：meta#240 聊天话术泄露 sample_quantity 字段名 + Rf 全精度 — narrate 上下文根源修

你是 S3（先定位产出链，再根源修，列车口径）。任务书 = c12-ai/BIC-meta#240（读正文：两缺陷、修复方向、验收）。

仓：BIC-agent-service，从 origin/main（≥8b2afc0）切工作树 `.wt/be240`，分支 `fix/issue-240-narration-field-leak`。

## A. 先定位（结论评论 #240）

1. 该 CC 参数追问消息的产出链：narrate 改写器（#194 单源 emit_prejob_narration/_narrate_closing 一族）还是别的出口；narrator 上下文里表单 schema 字段名（sample_quantity）与原始 Rf 浮点是怎么进去的（file:line）。
2. 全量盘点两类泄露面：(a) 会回显 snake_case 字段名的话术场景（所有表单追问/缺参提示）；(b) 含 Rf 的用户可见叙述面（#212 的 format_rf 目前只接了失败收尾——列出未接面）。

## B. 根源修（不打输出端字符串补丁）

- **上下文治理**：进 narrator 的字段引用用业务名/display label（或明示『内部字段名禁止外显，用业务称谓』的强约束——优先数据侧治理，提示词约束为辅）；数值（Rf 等）在进上下文前经确定性格式化（format_rf 单源复用）。
- 与 #212/#227 刚落的叙述面无回归（既有具名套件全绿）。
- 手术纪律：不重构 narrate 架构，#194 结构 grep 锁不破坏。

## 二元验收

- 产出链 file:line + 全量泄露面清单评论 #240；
- 具名负向断言：话术无 snake_case 字段名模式（\\b[a-z]+_[a-z_]+\\b 类，白名单注明合法术语）+ Rf 无 >3 位小数（扩展 #212 断言到新接面）；
- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#240 在 squash 正文 + 具名测试在 main）。
- **不重启台架**（部署归 root，跟 obj235/eln111 一个批次窗口）。

## 并行知会（同仓两个在跑，动前互 ack）

- s3-obj235（bic-orch:2.1）：L2 objective 轮次优化——它可能动 update_objective_params 工具面；你在 narrate 上下文面，若撞 prompt/上下文组装文件先 dispatch send 互 ack；
- s3-eln111（3.1）：app/eln/ 聚合器——无交集预期。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
