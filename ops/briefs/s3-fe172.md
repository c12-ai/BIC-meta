# S3 任务：BIC-meta#172 — 完成后导航去绕行 + 监控'结果分析中'段 + END_TLC 文案改收尾语义

你是 S3（实现 + PR，列车口径）。任务书 = meta#172（读全 issue）。

仓：BIC-agent-portal，从 origin/main（≥7d278d5）切工作树 `.wt/fe172`，分支 `fix/issue-172-analysis-segment-nav`。
并行知会：s3-trace165 在 chat 追踪块（无交集）。你动的面与 #159 的 derive-routing / step-nav-orchestrator 重叠——那是刚合的机制，**只修中间态路由缺陷，别推翻 #159 的四条已验收行为**（confirm→monitor、completed→result 改为 completed→monitor 分析段→analyzed→result、卡替换、手动切走不强拉回；其中第二条按 #172 的新期望修订，修订即产品裁定，径直执行）。

## 范围

1. **根因定位绕行**：trial completed 到 task_result_analyzed 之间，routing 为何先算出 任务tab/参数设计（大概率是下一步 step 的 params 面变 active 触发 derive-routing 中间态）。修法要一步到位：completed 时目标是**监控 tab**（不是结果、不是任务）。
2. **监控时间线'结果分析中'段**：completed 且未 analyzed → 监控时间线追加进行中条目（'实验完成，结果分析中…' spinner，i18n zh/en）；analyzed 到达 → 条目转完成 + 边沿门自动跳结果 tab。失败（turn_failed/分析报错）→ 条目转失败态，不跳转。快照重建（刷新）下该段状态与 live 一致（从事件推导，不靠瞬态内存）。
3. **文案**：`end_thin_layer_chromatography` zh '结束 TLC 分析'→'TLC 收尾'，en 对应 cleanup 语义。
4. #159 的既有具名测试：手动切走不强拉回保持绿；completed→result 一步跳的旧断言按新语义翻转（PR 描述说明）。

## 二元验收

- 时序具名测试：completed→监控分析段（无任务tab绕行）→analyzed→结果tab；分析失败→段失败不跳。
- 刷新一致性：completed-未analyzed 时刷新，监控仍显示分析中段（具名测试）。
- 文案替换 zh/en；typecheck/lint 0 warn/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

PR sha 评论 #172；dispatch done（FACTS/JUDGMENT 分开）。
