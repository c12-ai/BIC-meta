# S3 任务：meta#213 — FP 两区试管架统一右侧（微改）

你是 S3（实现 + PR，列车口径，小改动）。任务书 = meta#213。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe213`，分支 `ux/issue-213-fp-rack-side`。
并行知会：s3-fe46 在 TLC modal/MaterialPreparationPanel（不同面）、s3-fe201b 在 chat trace（无交集）；合并前 rebase 最新 main。

## 范围

FP 上游只读区改为 馏分列表左 + 蛇形架右（与容器分配区同侧同宽口径）；#173 蛇形/#174 着色/#176 交互零回退；窄视口降级与 #176 同款。

## 二元验收

两区架子同右（截图实证）；既有具名测试绿；typecheck/lint/vitest/build + portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

PR sha + 截图评论 #213；dispatch done（FACTS/JUDGMENT 分开）。
