# S3 任务：BIC-meta#180 — TLC 下发后不自动跳监控（回归定位 + 修复）

你是 S3（先调查后修，列车口径）。任务书 = meta#180（读全 issue，含三个调查方向）。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe180`，分支 `fix/issue-180-dispatch-jump`。
并行知会：s3-fe179 在 FormChromeCollapse 骨架（`.wt/fe179`）——不同组件面；若你查到根因确在 collapse 吞事件（issue 方向3），STOP 评论 issue 并 dispatch ask 协调，勿双改同文件。

## 纪律

- 先复现（store 级时序测试或 CDP 自起实例，**不动台架 :5173**），根因结论评论 issue 再动手修。
- #159→#172 的行为演化史都在 issue/PR 里（portal#27、#38），先读，别推翻既有验收语义：手动切走不强拉回必须保持；你要修的是 dispatch→monitor 这条该跳没跳。
- 若根因是'表单流内常规操作被误记为手动切走'，修边界判定（什么算用户主动切 tab）并把边界写进具名测试。

## 二元验收

- 时序具名测试：确认下发 → 目标 tab=监控（等待实验开始态）；#172 四行为 + 手动切走语义回归测试全绿。
- typecheck/lint 0 warn/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

根因结论 + PR sha 评论 #180；dispatch done（FACTS/JUDGMENT 分开）。
