# S3 任务：main 止血小修 — TLC modal deselect 不再删库存（#206 定稿前的最小保护）

你是 S3（实现 + PR，列车口径，**极小改动**）。背景 = meta#206 F 段（一手 file:line）：main 的 `MaterialPreparationPanel.tsx:364,372-380` deselect 分支调 `maintenanceMutation({occupied:false})` → lab 硬删库存管——用户正常取消选中就丢台账数据。完整模型重构由 #46 一次性更新承接（另一会话），本任务只做止血。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe207`，分支 `fix/issue-206-deselect-hotfix`。

## 范围（KISS，一处）

- deselect 既有管 = 仅清本地选择状态，**零 lab 写入**（删掉/绕过该分支的 maintenanceMutation 调用）；注释更正为与行为一致（rule 8 (b)：existing-tube select/deselect never touches inventory——PRD 今日已增补）。
- 不动放置/维护/其他路径（那是 #46 一次性更新的事）；改动面越小越好，给 #46 的 merge 留最小冲突。

## 二元验收

- 具名测试：deselect 既有管后库存 API 零调用、管保持库存态（选中态清除）；连点两下 = 回到绿，不消失。
- typecheck/lint/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**（vite 热更归 root）。

## 收尾

PR sha 评论 #206（注明"interim hotfix，完整模型随 #46"）；dispatch done（FACTS/JUDGMENT 分开）。
