# S3 任务：meta#215 — 结果页轮次 IA 贯彻（tab 切内容、单 TLC 步骤卡、步骤序号回归工作流语义）

你是 S3（实现 + PR，列车口径）。任务书 = meta#215（读全 issue + 截图描述）。设计基准 = #128 FE-2 已验收 IA（轮=tab 三面共享、内容按轮切片）——结果面没贯彻：轮 tab 与三张平铺 TLC 卡并存、三轮各占步骤序号 1-3。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe215`，分支 `fix/issue-215-result-round-ia`。
并行知会：s3-fe213（FP 架同侧）、s3-fe201b（trace 间距）同仓不同面；s3-be216 在查实验级状态聚合（BE 面）——你只修结果面 IA，不动状态语义。

## 范围

1. 结果面 TLC 收敛为**一个步骤卡**，卡内轮内容随共享轮 tab 切换（复用 FE-2 的 selectedTrialByStage）；'已自动重试'历史轮在对应 tab 下可见。
2. 步骤序号回归工作流步骤序（TLC=1、过柱=2、组分收集=3、旋蒸=4）。
3. FE-2/#159/#172 既有具名测试处置说明（翻转/保留逐条）。

## 二元验收

- 三轮实验的结果面：单 TLC 卡 + tab 切轮生效 + 序号正确（具名 DOM 测试）；单轮实验无回退。
- typecheck/lint/vitest 全量/build + portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

PR sha + 截图评论 #215；dispatch done（FACTS/JUDGMENT 分开）。
