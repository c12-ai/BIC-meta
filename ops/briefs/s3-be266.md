# S3 任务：meta#266 BE 启动对账器遇 lab 404 拒启 — 吸收为终态而非全站瘫痪

你是 S3（高优根修，列车口径）。任务书 = c12-ai/BIC-meta#266（读正文：一日两案的一手证据、定性、修复要求、回归边界）。

现象：BE 重启时 reconciler 检测阶段对 lab `GET /tasks/{id}` 得 404 → `reconciler.reconcile detection-phase failure — exiting non-zero` → 无限崩溃循环，全产品不可用。今日台架与现场各瘫一次（lab `reset-to-test-data` 后 BE 库存留 dispatched trial 必踩），只能人肉 SQL 终态化解锁。

仓：BIC-agent-service，从 origin/main（≥9c0986e）切工作树 `.wt/be266`，分支 `fix/issue-266-reconciler-absorb-lab-404`。

## 修复口径（issue 已定，勿重议）

1. reconciler 检测阶段单条 trial 查 lab 得 **404** → 该 trial 标记终态（语义按 #128 三层状态择定：建议新值 `lab_task_lost` 或复用 `superseded`，写明理由）+ **WARN 留痕**（trial id、lab task id、动作）→ **继续启动**；
2. lab **整体不可达**（connection refused/timeout/5xx）→ 维持 fail-loud 拒启（依赖缺失 ≠ 单条数据漂移——这条是回归保护线，别顺手放宽）；
3. 多条 404 逐条吸收；吸收后的 trial 对用户面呈现遵循既有终态渲染（不新造 UI 状态）。

## 二元验收

- 具名测试：单条 404 → 启动成功 + trial 终态 + WARN（正向）；connection refused → 拒启（负向回归）；多条 404 混合 200 → 只吸收 404 条；
- 与 #128 状态机、#5 fail-accept、be216 rollup 无回归（既有套件全绿）；
- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455；MQ 集成族已知台架挂死，以 CI 隔离环境为权威门禁）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#266 在 squash 正文 + 具名测试在 main）；
- PR sha + 终态语义选择理由评论 #266。**部署归 root**（台架重启 + 现场镜像滚更，我会在两处各做一次"lab reset 后重启 BE"的实弹验证）。

## 并行知会

- BE 仓当前无其它在跑会话。portal 侧无关。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
