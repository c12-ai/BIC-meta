# S3 任务：meta#254 现场 SSE 持续断连横幅 — 先代码侧定分叉，再修

你是 S3（先调查后修，列车口径）。任务书 = c12-ai/BIC-meta#254（读正文：现场证据、四个分叉、现场访问纪律）。

现象：现场 portal 持续『连接已断开，正在重连…』；BE 全 200；断流约 10 分钟一次；ticket 高频重申。

仓：跨 BIC-agent-portal（banner/重连逻辑）与 BIC-agent-service（ticket TTL/keepalive）。工作树各自 `.wt/sse254`。

## A. 代码侧先定大半（结论评论 #254）

1. BE：sse ticket TTL、流最长生命周期、keepalive 间隔的设计值（file:line）；~10 分钟断流是否=设计轮转。
2. portal：banner 出现/清除条件；EventSource onerror→重连→banner 清除的完整路径；找出『重连成功但 banner 不清』或『重连静默失败』的分支。
3. 台架对照：同版本台架（localhost:5173→:8800）是否同样轮转但无持续 banner——差异点即根因所在。

## B. 现场证据（若需要）

只读纪律（违者即事故）：ssh orin-tail 仅允许 `docker logs` / `curl`（GET/HEAD）/ `rabbitmqctl list_*`；**禁一切写**（stop/rm/restart/compose/sed/文件写/env 改）。需要现场变更一律 dispatch ask 回报 root 执行。

## C. 修复口径

- TTL 轮转必须无感：先建新流再关旧流（或静默重连），banner 只在真失联（重试超阈值）时出现；
- 具名测试：轮转不出 banner（负向）+ 真失联出 banner + 恢复后清除；
- BE 侧若改 TTL/keepalive：写明依据，别拍脑袋放大；
- 两仓各自 PR、CI 绿、admin squash-merge、内容核验。**部署归 root**（portal 需重建 field 镜像；BE 滚更）——你 merge 后 done 即可。

## 并行知会

- s3-fe252（bic-orch:2.1）在 portal 修 PKCE 垫片（入口文件），与你可能撞 `main.tsx`/启动路径——动前 dispatch send 互 ack；
- s3-be255 在 BE narrate 面（无交集预期）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开；分叉结论 + 双仓 PR sha）。
