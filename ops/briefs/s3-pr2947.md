# S3 任务：portal#29 + portal#47 更新→复审→合并（Wenlong 裁定 2026-07-11，解除无-reviewReq 限制）

你是 S3（PR 列车收尾，列车口径）。用户裁定原话：『portal 29, 47我们来rebase main， reviw， update & merge』——两个 PR 由我们代更新、复审、合并。前情：meta#209 台账（两 PR 首轮审查结论在内）+ portal#46/#206 裁定 + 今日已合五个 portal PR（#57/#58/#59/#60/#61，main HEAD ≥1c28467）。

仓：BIC-agent-portal。**纪律：不 force-push 同事分支**——一律 `merge origin/main` + stacked commits 推分支（squash 合并后线性历史无所谓）。每个 PR 合并后做内容核验（issue/PR 号在 squash 正文 + 关键变更在 main）。

## A. portal#29（c12-syq，TLC 耗材维护控件）——先做

1. 工作树 `.wt/pr29` checkout PR 分支，`git merge origin/main` 解冲突。冲突面预警：#46 staged placement（MaterialPreparationPanel）、fe220 类型 chip + locales、#223 StepStrip、#225 badge——**以 main 语义为准**，#29 只带入它自己的两点：rule-7 `shouldShowAreaActions` 对 sample_tube 抑制 Fill/Clear + rule-1 显示层过滤 tlc_developing_tank/tlc_silica_plate。
2. 合并后复审：首轮 verdict（#209：rule 1/7 符合，通过）在新 main 语义下是否仍成立——特别核对与 #46 的『Consumable Maintenance 只读特殊品』和 fe220 的类型显示无互斥；非阻断项（updateWorkspaceAreaSlots 串行写无回滚）保持非阻断，评论提醒即可。
3. 四门（tsc/biome/vitest/build）+ CI 绿 → admin squash-merge；PR 评论记录『Wenlong 裁定 2026-07-11 代合』+ 复审结论。

## B. portal#47（DrakeZhou97，Biome format + organize-imports sweep）——#29 合完再做

1. **重新生成而非机械 rebase**：checkout PR 分支 → merge 最新 main（含刚合的 #29）→ 在合并树上重跑 `biome format --write` + organize-imports（用仓里 CI 同款命令）→ 提交为 stacked commit。
2. 验证零逻辑变更：git diff 仅格式/import 序；tsc + vitest 全绿佐证。
3. CI 绿 → admin squash-merge；评论同 A.3。

## C. 收尾

- meta#209 台账评论：更新 C 组两行（#29/#47 已按用户裁定代合，sha 各附）；提醒 c12-syq lab#111 仍待其自身节奏。
- **不重启台架**；portal 部署归 root。
- dispatch done（FACTS/JUDGMENT 分开；两 PR 各自 merge sha + 内容核验结果 + 复审 verdict）。

## 并行知会

- s3-eln229 在 BIC-agent-service（不同仓，无交集）。portal 侧无在跑会话。
