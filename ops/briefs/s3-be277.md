# S3 任务：meta#277 化合物名清空复发 — 深入复现 + 全写路径收敛

你是 S3（先远端复现定位，再修，列车口径）。任务书 = c12-ai/BIC-meta#277（读正文：首要嫌疑=be270 flag 的 REST /objective/draft 快路径残留）+ #270 的 PR#122（5e77288，_merge_reactant_rows 合并语义）。

**远端访问：一律用 `ssh orin-tail`（tailscale；LAN 别名 orin 当前不可靠）。只读纪律：只 select/docker logs/curl GET，禁一切写；需要现场变更回报 root。**

仓：BIC-agent-service，从 origin/main（≥6f0c75b）切工作树 `.wt/be277`，分支 `fix/issue-277-draft-fastpath-enrichment`。

## A. 深入复现（结论评论 #277）

1. 远端最新会话数据（talos_agent_db）+ BE 日志重建：哪个具体操作触发清空——表单双栏编辑保存（REST /objective/draft）？还是其它轮次；抓触发前后 reactants JSON 对照与对应请求；
2. 核对快路径 file:line：为何绕过 _merge_reactant_rows；顺带**清点所有 reactants 写路径**（工具/REST/reseed/确认兜底…），逐条标注是否走合并权威——清单落 issue，杜绝第三次漏；
3. 排除部署因素：确认触发路径确实执行的是 5e77288 后代码。

## B. 修复

- 快路径接入同一合并/富化权威（单点）；全写路径结构断言（测试枚举所有写点均经权威函数）；
- 具名测试：REST 草稿保存不冲富化（正向）+ smiles 变更不串富化（负向回归）+ #270 既有 8 测不回退；
- 全量 pytest（DB 5455 串行；MQ 族以 CI 为权威）+ ruff/pyright 0 + CI 绿 → admin squash-merge，内容核验（#277 在 squash 正文）；
- 复现结论 + 写路径清单 + PR sha 评论 #277。**部署归 root**。

## 收尾纪律

dispatch done 用**单行短摘要**（长版落 issue 评论），调用后核对退出码 0——fp275/be273 踩过多行参数解析失败的坑。

## 并行知会

- 同期有 issue 清理会话（s3-sweep，meta 仓 issue 操作，无代码交集）。BE 仓无其它在跑。
