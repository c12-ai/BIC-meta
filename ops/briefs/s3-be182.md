# S3 任务：BIC-meta#182 — 最终总结读错状态层 + 模板改受约束 LLM 叙述 + 排版粘连

你是 S3（先查后修，列车口径）。任务书 = meta#182（读全 issue）。前置已合：be181 的 executor_display_name 单源（BE main a66d542）——你的 stage 名一律走它，勿再造映射。

仓：BIC-agent-service，从 origin/main（≥a66d542）切工作树 `.wt/be182`，分支 `fix/issue-182-final-summary`。
并行知会：s3-eln183（ELN 报告面）、s3-fe176b（FP 表单/payload 面）在跑，与你的话术总结面无交集；合并前 rebase。

## 范围

1. **读层修正（先调查）**：复现全流程结束总结，定位步骤终态读的哪一层；改为 **jobs.outcome × review**（#128 三层语义）：重试后达标 → 达标/已完成；真 fail-accept → 如实失败。结论评论 issue 再动手。
2. **模板 → 受约束 LLM 叙述**：终态集合从 jobs 层取出后作为不可改写事实喂入 narrate（走既有单产出口架构），LLM 只组织语言；具名测试断言输出含逐步骤真实终态（正向）且无越权事实断言（负向，吸取 #117/#158 教训）；LLM 不可用时回退确定性模板（fail-safe 保留）。
3. **粘连修复**：'已完成 所有步骤结果均已确认…' 双段拼接无分隔的装配点修正（大概率与 ELN 提示句拼接有关）。

## 二元验收

- 具名测试：重试达标夹具 → TLC 显示达标；fail-accept 夹具 → 如实失败（对照）；LLM 叙述含全部真实终态、无编造、无粘连；回退路径测试。
- 全量 pytest 绿（真 CI）；ruff/pyright 干净；CI 绿 admin-merge 留痕。**不重启台架**（部署归 root）。本地验证用 talos-pg-test-73:5455。

## 收尾

读层结论 + PR sha 评论 #182；dispatch done（FACTS/JUDGMENT 分开）。
