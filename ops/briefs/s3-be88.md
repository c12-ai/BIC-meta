# S3 任务：BIC-agent-service#88（P1）— 解除 CI 对整个 DB-backed 套件的静默跳过

你是 S3（实现 + PR，列车口径）。任务书 = c12-ai/BIC-agent-service#88（读全 issue：根因、13 红分类、CI services 方案讨论）+ #86 的发现语境（issuecomment-4933579525）。

仓：BIC-agent-service，从 origin/main（≥b9ca8ee）切工作树 `.wt/be88`，分支 `ci/issue-88-unskip-db-suite`。

## 背景（已核实的事实）

- `.github/workflows/test.yml` 自首个提交（3d10b21）起 `PG_DATABASE=template_test_db`，而 `_l4_engine` fixture 要求 `template_db_test`——名字错位导致整个 DB-backed 套件在 CI pytest-skip（当前 570 skipped）。
- 改对名字后全套件真跑，暴露 13 个既有红：auth-401 ×10（需 Keycloak）、live-MinIO ×1（test_l4_e2e_turn）、事件形状漂移 ×3。本地全量（真测试库 talos-pg-test-73:5455）基线：13 FAILED / 2138 passed。

## 范围（按此优先序，每步独立可验）

1. **修正 PG_DATABASE 拼写错位**（一行）。
2. **CI services 落地**：workflow 加 postgres service（或沿用现有 pg 配置）+ **Keycloak service 容器**（quay.io/keycloak start-dev + realm 种子，参考 meta scripts/bic-env/up.sh 第 5 节的 seed 口径与 BE .env.test 期望的 issuer）+ **MinIO service**（minio/minio + 建桶）。测试侧若已有内嵌 stub 路径（查 conftest / .env.test.example），优先用轻的——issue 里两案任选，以"CI 时长可控 + 不引入 mock 语义漂移"为准。
3. **修 13 个既有红**：auth-401 ×10（多半是 fixture 缺 token 铸造——对照本地怎么过的：本地这些测试过吗？若本地也红则是真欠账，逐个修）；事件形状漂移 ×3（对齐现 main 的实际序列化，同 #86/#89 的跟修法）；test_l4_e2e_turn 的 MinIO 依赖（service 起了应自然绿，否则查桶/凭据）。
4. **护栏**：加一个 CI 步骤断言 skipped 数量低于阈值（如 <50），防"静默跳过"这类 fail-loud 违规再犯——阈值写成显式常量并注释理由。

## 红线

- 不许用 pytest.skip/xfail 掩盖任何一个红来换绿；修不动的红先评论 issue 报告等裁定。
- 运行时代码只在"测试暴露真运行缺陷"时才动，动之前评论 issue 报告。
- **不重启台架**；本地验证用 talos-pg-test-73:5455 测试库，别碰 bic-postgres:5432 的活库。

## 二元验收

- CI 上 DB-backed 套件真实执行（skipped 从 570 降到自然水平并有护栏断言），**全绿**；本地全量 pytest 绿。
- 13 红逐个在 PR 描述对账表打勾（修复方式一行说明）。
- CI 绿 admin-merge 留痕。

## 收尾

PR sha + 对账表评论 issue #88；dispatch done（FACTS/JUDGMENT 分开）。
