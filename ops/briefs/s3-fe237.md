# S3 任务：meta#237 物料 modal 刷新校验功能消失 — git 考古 + 按 staged 模型恢复

你是 S3（先考古定性，再修，列车口径）。任务书 = c12-ai/BIC-meta#237（读正文：背景关联 #19/#46/#206/#29/#220、恢复语义、验收）。

仓：BIC-agent-portal，从 origin/main（≥34e32e5）切工作树 `.wt/fe237`，分支 `fix/issue-237-restore-refresh-validation`。

## A. 先考古（结论评论 #237）

- 在 git 历史定位刷新校验控件的原实现（组件/按钮/query invalidation/mutation）与消失点（哪个 squash/PR 移除或失效）；判定有意移除（找得到裁定记录）还是重构遗失（回归）。
- 若在历史里找不到该控件——如实报告『历史上无此控件』并给出等价能力现状（modal 打开时拉的是什么快照、之后是否有任何再同步时机），这同样回答用户疑问。

## B. 恢复（按 staged 模型适配，不是原样回贴）

- 刷新 = 重拉 lab 实时库存 + 与当前 draft/选择对账：失效项（已选管被占用/删除、库存漂移）显式标记+提示（fail-loud），不静默覆盖 draft、不静默保留失效选择；#206 四不变量不破坏（刷新绝不触发库存写）。
- TLC 与 CC 面口径一致（右面板一致性要求）。
- 具名测试：刷新后失效选择被标记（负向）+ 未提交 draft 刷新后仍在（不丢用户工作）+ 刷新零库存写断言。

## 二元验收

- 考古结论（消失点 commit/PR 或『从未存在』）评论 #237；
- tsc/biome/vitest + CI 绿 → admin squash-merge，内容核验（#237 在 squash 正文 + 具名测试在 main）；
- **不重启台架**（部署归 root）。

## 并行知会

- portal 侧无在跑会话（fe226b 已收档）。s3-obj235/s3-eln111 在 BE 仓、s3-deploy1 在 meta/部署资产——无交集。
- ⚠️ 类型门控语义（没选纯/粗也能选已有管）是另一个待 Wenlong 裁定的设计题——**不在本任务范围**，别顺手改选择门控。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
