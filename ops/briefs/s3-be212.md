# S3 任务：meta#212 — TLC 失败收尾加引导 + Rf 小数格式化单源

你是 S3（实现 + PR，列车口径）。任务书 = meta#212（读全 issue）。话术层前情：#158 确定性达标收尾、#181 executor_display_name 单源、#182 finale、#194 pre-job 单源——你的改动落在**重试耗尽失败的确定性收尾模板**（#158 的镜像面），勿开新装配路径（#194 结构锁在，改既有模板内容即可）。

仓：BIC-agent-service，从 origin/main（≥89ac2bc）切工作树 `.wt/be212`，分支 `fix/issue-212-fail-guidance`。
并行知会：s3-pr94lint 在 guardrail 分支、无交集；s3-fe46/fe201b 在 portal。

## 范围

1. 重试耗尽失败收尾：追加两选项引导（右侧审核→accept 继续进过柱 / 调整参数重试），zh/en；语义对齐 #5/#129 fail-accept 行为与 #11 询问叙述。
2. Rf/数值格式化单源：一个 format 函数（2-3 位小数），盘点确定性模板 + narrate 喂养上下文所有 Rf 出口统一消费；事件/DB 层不动。
3. 具名测试：失败收尾含引导（正向）+ 达标收尾不回退（#158 回归）+ 话术无 >3 位小数（负向）。

## 二元验收

全量 pytest + ruff/pyright + CI 绿 admin-merge 留痕；**不重启台架**（部署归 root）。本地验证 talos-pg-test-73:5455。

## 收尾

PR sha 评论 #212；dispatch done（FACTS/JUDGMENT 分开）。
