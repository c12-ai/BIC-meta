# S3 任务：BIC-agent-service#98 — RE 思考语句索要已知信息（FP basis 未喂或 prompt 缺约束），按根因修

你是 S3（先调查后实现，列车口径）。任务书 = BE#98（读全 issue，含 PRD rule 11 依据）。红线：**按根因修，不打文案禁令补丁**（memory 纪律）。

仓：BIC-agent-service，从 origin/main（≥5327517）切工作树 `.wt/be98`，分支 `fix/issue-98-re-basis-context`。

## 调查先行（结论评论 issue 再动手）

- 复现路径：FP 结果确认 → RE turn。检查 RE specialist 图节点/prompt 组装：FP 确认结果派生的 RE 推荐依据（体积 = 15ml/管 × 管数、溶剂/比例解析——rule 11 的既有实现）是否进入了该 turn 的模型上下文？
- 两种根因分支：①basis 数据没喂进 prompt → 修数据管道（喂进去）；②喂了但模型仍征询 → prompt 加"上游依据已在上下文，直接使用勿向用户索要"的结构性约束（放在 specialist 系统段，不是对话内禁令）。
- 注意 #165 刚落的 reasoning 持久化：思考文本现在会存档并展示，reasoning 里的幻觉征询是用户可见问题。

## 二元验收

- 具名测试：FP 确认后的 RE turn，reasoning/正文无向用户索要体积/溶剂的征询（负向断言），RE 推荐依据字段来自 FP 派生值（正向断言，对齐既有 rule-11 测试族）。
- 全量本地 pytest 绿；ruff/pyright 干净；CI 绿 admin-merge 留痕。**不重启台架**（部署归 root）。本地验证用 talos-pg-test-73:5455。

## 收尾

调查结论 + PR sha 评论 issue #98；dispatch done（FACTS/JUDGMENT 分开）。
