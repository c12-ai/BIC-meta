# S3 任务：BIC-meta#179 — header 折叠全节覆盖 + 短表单抖动环根治 + 任务设置条压缩

你是 S3（实现 + PR，列车口径）。任务书 = meta#179（读全 issue）。这是 #175（FormChromeCollapse 骨架，portal ae5e61f）上台后的首轮实测反馈——**修在骨架层，别逐表单打补丁**。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe179`，分支 `fix/issue-179-collapse-followup`。

## 三项（一 PR 可容纳，逻辑上是同一骨架的三处收尾）

1. **全节覆盖**：滚动折叠驱动从 ParameterDesignLayout 上提/复制到实验目标、流程编排两节的容器（找 #175 落的 FormChromeCollapse context 消费点，缺哪节补哪节）。
2. **抖动环根治（重点）**：先在测试里复现环（短内容 + 滚动/尺寸事件序列 → header 状态无限翻转），再修。裁定方向（三选一或组合，PR 描述说明取舍）：
   - 内容高度门槛：`content scrollHeight - clientHeight < 折叠回收的高度差 + margin` 时禁用折叠（短表单根本不折叠，从源头无环）；
   - 迟滞：折叠阈值 > 展开阈值，且状态切换后冷却；
   - 锁存：折叠后不因"不可滚"自动展开，仅显式回顶/点击展开。
   验收断言：模拟事件序列下 header 状态在有限步内收敛（回退即红）。
3. **压缩**：任务设置标题条矮化；节标题+副标题行紧凑重排（副标题弱化/内联/tooltip 自选，PR 截图对比）。

## 二元验收

- 三节折叠行为一致（逐节具名测试）；短内容无振荡（收敛性具名测试）；高度下降截图对比。
- #175 既有测试按新行为处置逐条说明；typecheck/lint 0 warn/vitest 全量/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**（vite 热更归 root）。

## 收尾

PR sha + 前后截图评论 #179；dispatch done（FACTS/JUDGMENT 分开）。
