# S3 任务：meta#220 物料准备 modal — i18n 语料补齐 + 纯/粗品视觉区分

你是 S3（修复会话，列车口径）。任务书 = c12-ai/BIC-meta#220（读正文与截图描述）。
背景：portal PR#46（squash `daf5498`）落地 rule-8 受控放置（meta#206 四不变量），但：
① 类型按钮渲染原始 key `workspace.material.addPureSample`/`addCrudeSample`（语料未加）；
② 网格中已放置管子纯/粗品同色，类型放完即不可见。

仓：BIC-agent-portal，从 origin/main（≥3b2c50e）切工作树 `.wt/fe220`，分支 `fix/issue-220-material-i18n-type-visual`。

## A. i18n 全量对账（不止两个 key）

- 对 #46 引入的全部 `t('workspace.material.*')`（及其它新 key）逐个核对 `src/locales/en`+`zh` 定义；缺失全补（中文按产品口径：纯品/粗品；英文对应 pure/crude sample）。
- 门禁缺口一并堵上：`src/locales/translation-parity.test.ts` 只查 en↔zh 对齐——新增『代码中使用的 key 必须有定义』的覆盖测试（静态扫 `t('…')` 字面量 vs locale 树；动态拼接 key 可白名单注明），保证原始 key 不再带绿灯出厂。

## B. 纯/粗品视觉区分

- 网格内已放置 tube 按类型可视区分（不同色/纹样 + 图例说明），且与『选中/未选中』态正交可同时读出（选中态现为绿色高亮——设计需保证类型×选中 2×2 都可辨）。
- TLC 与 CC 面（同一 staged 模型）口径一致；类型信息来源用放置时 armed 的类型（draft 层已有），持久层若无类型字段则读现有 inventory 元数据——**不得为显示新造后端字段**；若持久数据确实无类型可读，旧数据显示为『类型未知』中性态并在 PR 里写明，勿伪造。
- 遵守 #206 四不变量：本任务纯显示层，不得触碰 Confirm/Cancel/库存写路径语义。

## 二元验收

- 台架截图级：两个按钮显中文语料；网格中纯/粗品肉眼可辨（jsdom 具名 DOM 断言替代亦可，但按钮文案必须有断言）；
- 新覆盖测试对『使用未定义 key』红、对现状绿（负向断言在案）；
- tsc/biome/vitest 全绿 + CI 绿 → admin squash-merge，内容核验（issue 号在 squash 正文 + 具名测试在 main）。
- **不重启台架**（部署归 root）；portal 热更由 root 做。

## 收尾

根因 + 全量缺失 key 清单 + PR sha 评论 meta#220；dispatch done（FACTS/JUDGMENT 分开）。
与在跑会话无交集预期（当前无其它在跑 S3）。
