# S3 任务：meta#194（P1 结构性）— 叙述归一回退设计与实现

你是 S3（**设计先行**，列车口径）。任务书 = meta#194（读全 issue，含 census 给出的设计约束）+ #195 母 census 语境。

仓：BIC-agent-service，从 origin/main 切工作树 `.wt/be194`，分支 `fix/issue-194-narration-fallback`。
并行知会：s3-p2sweep（机械批）与 s3-docsync（文档批）同仓不同面；narrate 相关文件若 p2sweep 也列了机械项，以你为准协调（dispatch send 对齐）。

## 纪律

1. **设计先评论 #194 再动手**：方案覆盖——回退语义（LLM 不可用/超时/越权输出时的确定性路径）、与 #117/#158/#182 已建立的"事实不可改写"格局的关系、单产出口不变式的保持；列出翻转/新增的具名测试清单。设计经得起"回退即红"检验。
2. 实现按设计走；spec（narrate-contract.md）同步（Rule10）。
3. 与今晚 be181/be182 的改动零回退（它们的具名测试保持绿）。

## 二元验收

- 设计评论在案；具名测试：正常路径/回退路径/越权拦截三态；be181/be182 回归绿；全量 pytest + CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

设计结论 + PR sha 评论 #194；dispatch done（FACTS/JUDGMENT 分开）。
