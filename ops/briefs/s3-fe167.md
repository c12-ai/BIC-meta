# S3 任务：BIC-meta#167 — 执行日志 op 行图标态修复 + op 名/状态人类可读文案

你是 S3（实现 + PR，列车口径）。任务书 = meta#167（读全 issue，含截图描述与验收）。背景：FE-2（portal c384e9f，meta#128）刚落的 op 级执行日志的首轮实测反馈。

仓：BIC-agent-portal，从 origin/main（≥c384e9f）切工作树 `.wt/fe167`，分支 `fix/issue-167-op-row-display`。
注意：s3-trace165 也在 portal 动追踪块（chat 侧组件）——你的改动面在 workspace 执行日志组件，理论无交集；开工前 `git log origin/main` 看一眼有没有它先合的 PR，有就基于最新 main。

## 范围

1. op 行状态→图标映射：op_completed→绿勾，op_failed→红叉，仅进行中→loading。查清现状为什么全 loading（多半是状态字段没接到图标组件）。
2. op 名与状态的 i18n 文案（zh/en）：以 shared-types 45032c9 / lab main 实际 op 枚举为准全量盘点（agv_move、common_reset、reset、pick、place 等），映射表 + 未知类型诚实回退原始码（不造翻译）。
3. 状态词文案化（已完成/失败/进行中），去掉 \`op_completed\` 裸串。
4. 时间戳展示策略：终态批写导致同秒是数据真实——可选择对同秒批写组只显示组级时间；**不许伪造递增时间**。改不改自行判断，改了要在 PR 描述说明。

## 二元验收

- 三态图标具名测试；全量 op 类型 zh/en 文案 + 未知回退具名测试。
- typecheck / lint 0 warn / vitest 全量 / build 全绿；portal CI 绿 admin-merge 留痕。
- **不重启台架**（vite 热更由 root 拉取时自然生效，部署归 root）。

## 收尾

PR sha 评论 #167；dispatch done（FACTS/JUDGMENT 分开）。
