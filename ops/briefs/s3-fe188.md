# S3 任务：BIC-meta#188（P1）— CC 样品柱选择双重违反 rule 8，全链阻塞修复

你是 S3（实现 + PR，列车口径）。任务书 = meta#188（读全 issue，含截图/代码/git 三重取证）+ PRD rule 8 原文（maintain-then-select，2026-07-05 裁定）+ #190 走查台账语境。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe188`，分支 `fix/issue-188-cc-rule8`。

## 产品口径（PRD 权威，勿重议）

- rule 8：分配 = **点选已维护的既有物理项**；选择**绝不创建库存**；空 slot 只在**维护模式**填充/清空。
- rule 2/3（Experiment Item Management）：CC 样品柱是实验特定项，其维护模块含右上角维护模式按钮（同 Consumable Maintenance 模式），选择与维护是两个模式。
- 参照实现：**TLC 样品管选择是 rule-8 正确的**（e2ewalk 证实）——把 CC 对齐到 TLC 的交互模型，勿另造。

## 范围

1. 回滚/重写 c3855f2（#21）引入的 insert-and-assign 语义：占用 slot 点击=选中分配给当前任务；空 slot 常规模式不可点（或提示进维护模式）；维护模式恢复（若 #21 删除了维护入口，按 rule 2 的右上角按钮模式恢复；先查 #21 的完整 diff 判断删了什么）。
2. readiness 死锁场景回归：seed 占用 slot1 → 选择 → readiness 满足 → 可下发（具名测试）。
3. 与 TLC 的一致性：两处 rule-8 交互（占用可选/选择不建库/空slot维护限定）用共享逻辑或至少共享具名测试模板锁死。
4. 注意 c3855f2 是同事的 PR——PR 描述客观引 PRD rule 8 与 #188 取证，对事不对人。

## 二元验收

- 占用 slot 可选中分配（具名交互测试）；选择路径零库存创建（断言 API 调用面）；空 slot 常规不可点 + 维护模式可维护；readiness 死锁场景转绿。
- 全量 vitest + typecheck/lint/build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**（部署归 root，root 部署后在真链上补验 #98/#182/#183/#180/#179/#172/#167）。

## 收尾

PR sha 评论 #188；dispatch done（FACTS/JUDGMENT 分开）。
