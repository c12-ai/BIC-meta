# S3 任务：BIC-meta#189（P2）— bench-local Playwright spec 漂移规范化

你是 S3（实现 + PR，列车口径）。任务书 = meta#189（读全 issue：talos-postgres→bic-postgres、OIDC :8080→:18080、tube add-mode、eluent 直填→recommend-button 四处漂移，e2ewalk 已本地适配未提交）。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe189`，分支 `test/issue-189-spec-normalize`。
背景裁定演化：specs 曾裁"bench-local 不提交"（因硬编码台架容器 id）；本次目标是**去 bench 特定化后收编入仓**——环境值全部走 env/config（PG 容器名、OIDC authority、base url 等 default + env override），不再有硬编码 id，从而可提交。台架工作区的未提交适配版（tests/helpers.ts、tlc-retry-flow、tlc-e2e-final-chain、cc-re-chained 等）是起点素材——**只读参考台架工作区文件，别动台架工作区**。

## 范围

1. helpers/spec 环境参数化（PG 容器名 default bic-postgres、OIDC default :18080、可 env 覆盖）；四处漂移修正收编。
2. fp-chain-flow 的 idle-disabled 旧断言按 #176 新契约翻转。
3. Playwright 仍不进 CI（#160 裁定不变，workflow 注释已有）；但 spec 入仓后团队可本地跑。README/注释写清运行前置（活台架 + 过渡档 + reset）。
4. 与 s3-fe188 并行知会：它在 MaterialPreparationPanel 产品码，你在 tests/——若你的 spec 断言依赖它修后的 CC 语义，相关断言以 #188 合并后行为为准（可等它先合再 rebase，或该断言留 TODO 引 #188）。

## 二元验收

- 收编的 specs 无任何硬编码台架特定值（grep 断言：无 talos-postgres/:8080/容器 id 字面量）；helpers 单测（若有测试面）绿。
- typecheck/lint/build 绿（spec 文件纳入 lint 面）；portal CI 绿 admin-merge 留痕。**不动台架**。

## 收尾

PR sha 评论 #189；dispatch done（FACTS/JUDGMENT 分开）。
