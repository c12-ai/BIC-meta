# S3 任务：meta#258 会话列表命名 — 反应名/目标摘要 + 时间回退

你是 S3（先查数据源再实现，列车口径）。任务书 = c12-ai/BIC-meta#258（读正文：设计要求、不伪造原则、验收）。

现象：左侧会话列表显示『Session e634e0』UUID 前缀。裁定：用反应名/目标摘要 + 时间等有意义信息。

仓：BIC-agent-portal 为主，从 origin/main（≥8bcd7b8）切工作树 `.wt/fe258`；若需 BE 补字段则 BIC-agent-service（≥18efcd6）同名工作树。

## A. 先查数据源（决定单仓还是跨端）

- portal session 列表消费的 API 响应现有字段（file:line）：有没有实验名/objective 摘要/最近活跃时间；
- 不够 → BE session 列表接口补字段（如 display_hint：实验名或 objective 摘要 + last_active_at）——跨端契约，**rule 10：同步 spec 文档**（.trellis/spec 对应契约文件），FE/BE 同批落地。

## B. 显示规则（裁定口径）

1. 有实验/已确认目标：显示反应名或目标摘要（如实验总览的 rxn 名）；
2. 空会话：时间回退（『07-11 16:42 的会话』样式，zh+en locale）；**绝不伪造**名称；
3. 名称随会话推进自然更新（目标确认后占位→反应名）；『今天』分组保留；hover/次行带具体时间。

## 验收

- 两态具名测试（有实验名 / 时间回退）+ 更新态断言；
- portal 四门 + CI 绿；若动 BE：全量 pytest + CI 绿 + spec 同步在同一变更集；
- admin squash-merge + 内容核验（#258 在 squash 正文）；PR sha 评论 #258。
- **部署归 root**（台架热更 + 现场 field 镜像重建滚更）。

## 并行知会（portal 侧两个在跑，动前互 ack）

- s3-fe252（bic-orch:2.1）：PKCE 垫片（启动路径/main.tsx）；
- s3-sse254（3.1）：SSE banner/重连（可能动 SSE hook/连接层）；
- 你在会话列表组件/数据层，预期无交集；撞了 dispatch send 协调。
- s3-be255（4.1）在 BE narrate 面；若你也动 BE（session 接口），无交集预期，仍互 ack。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
