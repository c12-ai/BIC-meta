# S3 任务：BIC-meta#170 — 过柱结果证据面对齐设计（全 75 管架 + 真实 UV 图，撤硬编码假曲线）

你是 S3（实现 + PR，列车口径）。任务书 = meta#170（读全 issue，含设计对照与验收）。

仓：BIC-agent-portal，从 origin/main 切工作树 `.wt/fe170`，分支 `fix/issue-170-cc-evidence-design`。
并行知会：s3-trace165（chat 追踪块）、s3-fe167（执行日志 op 行）、s3-fe168（证据图 lightbox）都在 portal。**与 fe168 有真交集**（同在证据图渲染面）：你落地真实 UV 图 `<img>` 时按 #168 的 lightbox 接入口径写（若它先合，直接复用其组件；若你先合，留好接入面并在 PR 描述注明）。开工前 `git fetch` 基于最新 origin/main，合并前再 rebase 一次。

## 范围（#170 三条中的前两条；保留时间列不动——那是 #127 Mind 侧欠账）

1. **全 75 管试管架视图**：复用 FP 源试管选择器的 5×15 蛇形布局组件（只读态），分配管按角色着色（产物/疑似/废弃液），未分配灰显空闲；数据源 = CC 结果各峰的 rack_id/tube_position/role。
2. **真实 UV 光谱区**：删除 `ResultStageEvidence.tsx:381` 与 `:480` 的硬编码 polyline 假曲线；渲染 Mind 真图（优先 `annotated_pic_urls[0]`，回退 `pic_urls[0]`），同 TLC 板图的 presigned `<img>` 模式；加载失败/无图显诚实空态（不显假曲线）。检查 FP 证据面 :480 那处同类假曲线的语义——若 FP 无对应真图数据源，改为诚实空态并在 PR 描述说明。
3. 数据可得性自查：确认 BE 快照/事件里已把 `pic_urls`/`annotated_pic_urls` 传到 FE（BE cc/result 响应里有）；若 BE 没透传，这是 BE 小改（透传字段），先在 issue 评论范围再动 BE（契约变更走 rule 10 spec 同步）。

## 二元验收

- 全 75 管渲染 + 角色着色具名测试；未分配灰显。
- 代码中不存在硬编码光谱点串（两处 polyline 删净）；真图渲染 + 失败空态具名测试。
- typecheck / lint 0 warn / vitest 全量 / build 绿；portal CI 绿 admin-merge 留痕。**不重启台架**。

## 收尾

PR sha 评论 #170；dispatch done（FACTS/JUDGMENT 分开）。
