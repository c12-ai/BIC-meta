# S3 任务：meta#227 收尾叙述泄露内部步骤索引 — 结论不写『步骤 n』

你是 S3（先定位后修，列车口径）。任务书 = c12-ai/BIC-meta#227（读正文，含用户裁定原话）。

现象：全流程完成收尾叙述输出『TLC（步骤 0）、过柱（步骤 1）、组分收集（步骤 2）及旋转蒸发（步骤 3）均已标记为已完成…』。
裁定：**结论直接列阶段名，不带任何『步骤 n』**；0-based 索引是实现细节，任何用户可见叙述不得出现。

仓：BIC-agent-service，从 origin/main（≥e26096b）切工作树 `.wt/be227`，分支 `fix/issue-227-no-step-index-in-narration`。

## A. 先定位（结论评论 #227）

- 判定文案来源：narrate 改写器（LLM 上下文携带内部 step index 且未禁止外显）还是确定性模板/finale 组装。锚点：#194 narrate 单源（emit_prejob_narration / _narrate_closing.py 一族）、结构 grep 锁。
- 若是 narrator 上下文问题：**根源修上下文治理/提示词**（进 narrator 的阶段列表不带索引，或明示索引仅内部使用禁外显）——不打输出端字符串补丁（bic 工作流纪律：根源修复）。
- 顺查其它用户可见叙述面同类泄露（0 起步 step/job 编号、内部 id），清单落 #227。

## B. 修复+验收

- 同场景收尾叙述：四阶段名完整点到、无『步骤 n』模式（具名负向断言，正则含 0 起步检测『步骤\\s*0』及通用『步骤\\s*\\d』）；
- 与 #212 刚落的 tlc_result_review_fail_narration / finale 面无回归（既有套件全绿）；
- 注意区分：#215 结果面的 TLC=1/CC=2/FP=3 编号是产品设计（portal 侧），聊天结论不写编号——别把那套编号引进来。

## 并行知会

- 同仓在跑：s3-gate223（bic-orch:4.1，若走 BE 分叉动 L3 门控）——narrate 面与门控面预期无交集，撞文件先 dispatch send 互 ack。
- portal 侧三个在跑（fe220/fe222/fe225 批）与本任务无关。

## 二元验收

- 全量 pytest（-m 'not real_llm and not requires_bench'，DB=talos-pg-test-73:5455）+ ruff/pyright 改动文件 0 + CI 绿 → admin squash-merge，内容核验（issue 号在 squash 正文 + 具名测试在 main）；
- 定位结论 + PR sha 评论 #227。**不重启台架**（部署归 root）。

## 收尾

dispatch done（FACTS/JUDGMENT 分开）。
